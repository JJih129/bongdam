# 구조 정리 (v399, 2026-09-12) — 한 것 · 안 한 것 · 다음에 할 것

블록 270개 적층 구조의 정리 기록. 원칙: **동작은 바꾸지 않는다.** 순서·중복·죽은 코드만 손댄다.
회귀 게이트: `qa.cjs all` · `s_polish_v399` · `s_split_v399` · 데몬(체인 마커·조사→전투·시설 모달) · 완주 런.

## 1. 조사에서 나온 숫자 (정리 전)

| 항목 | 값 |
|---|---|
| 블록 | 270 (js 215 · css 55), 소스 3.69MB |
| 영구 폴링 setInterval | **179개 ≈ 초당 523회** 호출 (공용 틱 `BD_addTick`(0004)은 29블록만 사용) |
| 같은 전역을 여러 블록이 정의 | 77개 — `__bdNavOverride` 11블록, `BD_hazardInteract` 6, `BD_DAMI.show` 5, `BD_openSafetyMap` 5 … |
| 정의 없이 호출되는 전역 | 8개 (전부 typeof/&& 가드 있음 — 실해 없음) |
| 코드 0줄 툼스톤 블록 | 0071 · 0133 · (마커) 0100 |
| 서로 싸우는 루프 | 0190↔0263 (지도 버튼 700ms 생성 vs CSS 숨김), 0097↔0245 (스테이지 재등록 vs 2s 삭제), 0143 (700ms 정리 vs 툼스톤) |

## 2. 한 것

### 2-1. 죽은 코드
- 툼스톤 3블록 삭제 (파일 + shell 태그). **번호 재배열 없음** — 번호 공백은 bundle.js 가 허용하고, webbuild 의 STRIP 목록도 그대로 유효.
- 0019 터치 HUD 폴링에서 대시(v379)·허수아비(v381)·마법사 속성(v381) 구간 삭제.
- 0204 첫 줄이 `return;` 인 루프, 0190 레거시 지도 버튼 생성 루프 삭제. 0143/0245 정리 주기 완화(700ms·2s → 5~15s).
- 칩튠 BGM(v380 제거) 죽은 참조 2블록 정리.

### 2-2. 래퍼 체인 → «순서가 코드에 적힌» 블록
문제의 본질은 «누가 먼저 감쌌는가»가 **프로브 타이머 순서**로 결정돼 어디에도 적혀 있지 않았다는 것.

| 체인 | 종전 | 이제 |
|---|---|---|
| `BD_hazardInteract` | 0202·0219·0230·0240·0250 이 각자 300ms 프로브 (0240·0250 은 동기 설치) | **0271** 한 블록, 순서 명시: 폐기 → 재시도 → 기록 → 게이트 → 이중진입 → 원본 |
| `BD_DAMI.show` | 0238 조정자 위에 0152·0155 (200ms)·0191 (800ms) | **0272** 한 블록: 중복 억제 → 각성 재시도 → 효과음 → 조정자 |

원 블록에는 «(v399) 0271/0272 체인으로 이관» 표식만 남겼다. 되돌리려면 git 으로 그 자리만 복원한다.

## 3. 하지 않은 것 (사용자 «전부» 지시 대비 남은 범위)

| 항목 | 왜 미뤘나 | 하려면 |
|---|---|---|
| 폴링 루프 → `BD_addTick` 일괄 이관 (약 150개) | 틱 해상도 200ms 라 250·300ms 루프는 400ms 로 굵어져 **타이밍 회귀 위험**. CPU 이득은 실측상 미미(콜백당 수 µs) | ≥1000ms 루프(약 40개)만 먼저, 완주 런으로 확인 |
| `__bdNavOverride` 11블록 정리 | 함수가 아니라 **상태 플래그** 쟁탈 — 소유자를 정하는 설계 결정이 필요 | 길안내 소유 블록 하나(0121 또는 0240)로 쓰기 통일, 나머지는 요청 API 로 |
| `BD_openSafetyMap` 5정의 | 0208(신 지도)이 실질 소유. 0053/0180/0225 는 래핑. 0177 은 STRIP | 0177 삭제 + 0180/0225 래퍼를 0208 안으로 |
| 모바일 v398 레이어 14개 병합 | 전부 IIFE 라 병합은 «파일 합치기»뿐 — 이득이 가독성뿐이고 shell id 참조 유무 검증이 필요 | 필요할 때 `newlayer` 로 새 블록 하나에 이어 붙이고 옛 블록 삭제 |
| **엔진 재구성** (0017 6318줄·0053 3925줄·0051 2875줄) | 최상위 스코프 전역을 `<script>` 경계로 공유하고 `$`(14블록)·`renderMap`(13블록)·`p`·`locked` 이름이 충돌 — 단순 병합은 SyntaxError. 주 단위 작업 + 인게임 에디터(0023~0049 STRIP 블록) 호환 검증 필요 | 별도 스프린트. 순서: ① 전역 이름 목록 확정(0015 선언부) ② 0017 을 렌더/입력/스테이지/HUD 4모듈로 «파일만» 분리(스코프 유지) ③ 완주 런 3회 |

## 4. 규약 (유지보수용)

- 새 기능은 여전히 **새 레이어 블록**(`newlayer.js`). 다만 같은 함수를 감쌀 때는 **0271/0272 처럼 «순서를 적은 체인 블록» 하나에 추가**한다. 프로브로 각자 감싸지 않는다.
- 블록을 지울 때는 파일 + shell 태그를 함께, 번호는 비워 둔다.
- `deadsweep.cjs`(저장소 밖 `bongdam/tools`)로 폴링·중복 정의·죽은 참조를 다시 셀 수 있다.
