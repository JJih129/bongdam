# 봉담 안전지도 대작전 — 개선 루프 프롬프트

아래 «프롬프트» 블록을 통째로 복사해 Claude Code 에서 `/loop ` 뒤에 붙여 실행한다.
매번 깨어날 때마다 «1사이클 = 개선 → 검증 → 기록 → 새 제안»을 한 번 수행하고, 끝나면 곧바로 다음 사이클을 예약한다.
(`/loop` 없이 그냥 붙여 넣으면 한 사이클만 돈다.)

---

## 프롬프트

봉담 안전지도 대작전(봉담지킴이) 개선 루프를 한 사이클 수행해라. 사이클 = 개선 → 검증 → 기록 → 새 제안. 확인 질문 없이 진행하고, 결정이 필요한 것만 «결정 대기»에 적어라.

### 환경 (변하지 않는 사실)
- 리포 `C:\Users\ikh33\source\bongdam\repo`, 브랜치 `source`. `main` 은 빌드 산출물 전용. **배포(`node publish.js`)와 푸시는 내가 «배포해»라고 말하기 전엔 절대 하지 말 것 — 로컬 커밋만.**
- 소스 = `src/shell.html` + `src/blocks/NNNN_*.js|css`(번호 순 레이어, 뒤 블록이 앞 블록을 감싼다). 새 기능은 새 번호 블록(0278부터)으로 만들고 `src/shell.html` 끝의 `</body>` 앞에 `<script id="…">@@BLOCK:NNNN@@</script>` 한 줄을 추가한다. 기존 함수 재정의보다 래퍼·CSS·인터벌을 쓴다.
- 빌드: 웹판 `node build.js 웹게시` · 개발 번들 `node 검수도구/tools/bundle.js src 봉담지킴이_게시용_v338_final.html` 뒤 `node 검수도구/restamp.js 봉담지킴이_게시용_v338_final.html` — 스탬프가 바뀌면 `src/blocks/0000_bd-fresh-bake-boot-v117.js` 의 `var BUILD` 를 같은 값으로 고치고 bundle 을 한 번 더 돈다.
- 로컬 서버 `node C:\Users\ikh33\source\bongdam\tools\serve.cjs 8788` (죽어 있으면 백그라운드로 다시 띄움). 웹판 검증 URL `http://localhost:8788/new/`.
- 검증 도구(`검수도구/`): `qa.cjs all`, `s_story_s_v399.cjs`, `s_pcfix_v399.cjs`, `s_wp5_v399.cjs`, `s_reviewfix_v399.cjs`, `s_batch6_v399.cjs`, 캡처 `s_shots_ui_v399.cjs --out=<dir>`, 갤러리 `tools/shots_gallery.js <dir> <out.html> --review=<json> --run="…"`, 완주 런 `cd 검수도구 && node drive.js s_fullrun.js --url=http://localhost:8788/new/ > fullrun_<tag>.log`(백그라운드, 약 30분, 로그 끝 `FINAL: {"cleared":true…}` 가 성공), 데몬 `bd.js boot|eval|tp|shot|quit`(eval 인자는 공백 금지 → base64+atob).
- 함정: `window.STAGES`·`window.QUESTS` 는 undefined(top-level const) → `typeof STAGES !== 'undefined'` 로 검사. 웹판은 에디터 JSON·베이크를 읽지 않는다(주민·위험요소 소스는 0099·0104·0097). 하네스·블록 파일은 CRLF — 패치는 Node 스크립트 파일로 하고 셸 heredoc 안에 역슬래시를 두지 않는다. 완주 런이 웹게시를 읽는 동안 웹게시를 다시 빌드하지 않는다. `git commit -a` 금지(검수도구/*.log 가 딸려 들어간다) — `git add -A -- src 검수도구/<파일> <문서>` 로 명시.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

### 백로그 (위에서부터 집는다. 끝낸 것은 이 파일에서 지우고, 새로 찾은 것은 중요도 순으로 끼워 넣는다)
1. (완료 v400 — gen_placement.js·0071·0099·0104) 남은 것: 시설(건물) 배치는 0097 v24 파이프라인이라 아직 에디터 이동이 웹판에 안 감 — 별도 항목.
2. 갤러리 검수 메모 미반영 40건 — (a) 창 5종(가방·임무·수첩·리포트·장비) 닫기 버튼·제목·블러 통일, (b) 안전지도 리 이름·% 칩을 타일 밖 상단 띠로, 마커 색을 범례와 일치, (c) 리포트 타일 빈칸, HP «100 / 100»·경험치 «0/100» 표기 통일, 정화 스티커/배지 스킬 카드 아이콘 구분, 우하단 «0/39» 라벨, 담이 말풍선이 «◀ 동화리» 표지를 가리는 위치.
3. 회귀 인프라: `검수도구/smoke.cjs`(위 스위트 6개를 순서대로, 5분 내) + 캡처 40장 기준선 픽셀 비교(`검수도구/shots_baseline/` 와 diff 비율 보고). 완주 런 스크립트에 «시설을 의도적으로 방문하는» 단계 추가.
4. 구조 정리 2차: 프롬프트 3벌(0017 캔버스·0181 DOM·터치 힌트)→1벌, 토스트 2벌(`#bd-toast`·`#bge-toast`)→1벌, 지역 진행도 2벌(0176·0180)→1벌, 폐지된 파티 잔재(BD_PARTY·partyState·동료 스킬) 삭제, `window.<top-level const>` 패턴 정적 lint 스크립트.
5. 스토리 B — 교훈 2회→1회(감사 대사의 «~하자» 표어 제거), 3채널 모순 정리(수칙 카드·담이·수첩 12종 표), 감사 시퀀스 13명 개성 본문.
6. 수업 모드: «짧게 플레이» 토글(장 완료 = 부탁 2건 + 핵심 시설 1곳), 활동 리포트 인쇄용 화면, 저장 코드 내보내기/불러오기.
7. 모바일 잔여: 타이틀 아트 가짜 버튼과 실제 버튼 겹침, 미니맵 패널 글자 크기, 조작 힌트 문장.
- 결정 대기(하지 말 것): docx 대본 이식(보스 대칭·클라이맥스·동료 합류), 3지선다 부활, 장 완료 기준 완화 기본값, 배포.

### 1사이클 절차 (상한 90분, 넘기면 지금까지 한 것으로 4·5·6 을 마무리)
1. 백로그 맨 위 항목 하나(작으면 2~3개)를 고른다. 시작 전에 `git status` 로 작업 트리를 확인하고, 서버가 살아 있는지 `curl -s -o /dev/null -w "%{http_code}" http://localhost:8788/new/` 로 본다.
2. 구현 → 바꾼 블록마다 `node --check` → 웹판 빌드.
3. 최소 검증: 변경 화면을 찍어 단언하는 Playwright 스크립트 1개(`검수도구/s_<이름>_v399.cjs`, 기존 s_batch6 형식)를 만들어 통과시키고, `qa.cjs all` + `s_story_s_v399` + 변경과 관련된 기존 스위트를 돌린다. 스토리·지도·관문·전투·저장 흐름을 건드렸으면 완주 런을 백그라운드로 돌리고 FINAL `cleared:true` 를 확인한다(도는 동안은 문서 작업만).
4. 기록: `출시노트.md`·`progress.md` 의 최신 항목에 3줄 이내로 추가, 관련 문서(`스토리_시나리오_개선안_v399.md`·`PC웹_개선제안_v399.md`·`구조정리_v399.md`) 표 갱신, 이 파일의 백로그 갱신, 커밋.
5. 새 제안: `s_shots_ui_v399.cjs --out=검수도구/shots_ui_<태그>` 로 다시 찍어 이전 캡처와 비교하고, 완주 런 로그·콘솔 오류·스크린샷에서 발견한 문제를 중요도 순으로 백로그에 넣는다. 3사이클마다 갤러리를 다시 만들어 아티팩트 `https://claude.ai/code/artifact/744fcf3b-16ac-42ca-8156-9e1b2cb9d0c1` 을 같은 URL 로 갱신한다.
6. 보고(4줄): 한 것 / 검증 결과(통과·실패 숫자, 완주 런 결과) / 새로 백로그에 넣은 것 / 결정 대기. 그리고 다음 사이클을 바로 예약한다.

### 중단 조건
- 완주 런이 연속 2회 실패하면 원인 분석을 보고하고 멈춘다.
- 백로그가 비고 새 제안도 없으면 멈춘다.
- 내가 «멈춰»라고 하면 즉시 멈춘다.
- 배포·푸시·파일 삭제(`rm -rf`)·`git reset --hard`·브랜치 변경은 하지 않는다.
