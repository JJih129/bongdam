# 봉담지킴이 — 작업 인수인계

**최종 빌드** `봉담지킴이_v147.html` (약 88MB 단일 HTML)
**이전 빌드** `봉담지킴이_v146.html` (원본 보존)
**작성일** 2026-08-12
**이전 이력** v49~v146 세션 기록

---

## 0. 프로젝트 개요

봉담 지역 청소년 안전교육용 웹 RPG. **단일 HTML 파일** 하나에 코드·이미지(base64)·데이터가 전부 들어 있다.

- 흐름: 타이틀 → 캐릭터 선택 → 프롤로그(문화의집 3층 101) → 배지 수여 → 엘리베이터
  → 와우리(212) → 상리(213) → 동화리(211) → 수영리(210) → 최종 보스(212) → 엔딩 → 결과 리포트
- 핵심 루프: 주민에게 부탁 받기 → 위험요소 조사 → 전투(미니게임) → 정화 → 보고·보상
- 지역 이동은 **도로 끝까지 걸어가면** 옆 리로 넘어간다 (`districtGates`)

---

## 1. 검수 도구

### 관측 API — 게임에 내장
```js
window.render_game_to_text()   // 현재 상태를 JSON 한 줄로
window.advanceTime(ms)         // 프레임 진행
```

### 이번 세션에서 새로 만든 검수 하니스
이전 세션의 `봉담지킴이_자동검수.py` / `봉담지킴이_완주검증.py` 는 폴더에 남아 있지 않아
**Playwright(Node) 기반으로 재작성**했다. (스크래치패드에 위치)

| 파일 | 역할 |
|---|---|
| `drive.js` | 크로미움 실행 → 파일 로드 → 시나리오 실행 → 스크린샷·콘솔 수집 |
| `lib.js` | 상태 덤프 / 입력 차단 판정 / WASD 이동 / 패널 탐지 |
| `path.js` | **게임의 `_collidesAt` 을 그대로 써서 BFS 길찾기** — 「진짜로 도달 못 하는 목표」를 판별 |
| `auto.js` | 오토파일럿 — 길안내 화살표를 따라가며 대화·전투·모달을 자동 처리 |
| `s_auto.js` | 처음~엔딩 완주 시나리오 |
| `s_final.js` | 최종 보스·엔딩·결과 리포트 집중 검증 |
| `s_panels.js` | 모든 UI 패널이 열리고 **확실히 닫히는지** 검증 |
| `s_save.js` | 저장 → 새로고침 → 이어하기 왕복 검증 |
| `s_wallscan.js` | **투명벽 스캐너** — 콜라이더가 본체에서 떨어져 나간 오브젝트 전수 검출 |
| `s_quest.js` | 주민↔위험요소 부탁 매칭·고아 위험요소 검출 |

실행:
```bash
node drive.js s_auto.js          # 완주
SHOTS_DIR=shots_p node drive.js s_panels.js
```

---

## 2. v147에서 고친 것 — 진행 막힘 (가장 중요)

### ⚠ 2-1. 모든 모달이 0.5초 만에 저절로 닫히던 치명적 버그
v145 `bd-choice-leak-guard-v145` 의 «유령 모달» 판정이
`!m.offsetParent` 를 «안 보이는 상태»의 근거로 썼다.
그런데 `.bd-modal` 은 `position:fixed` 라 **offsetParent 가 항상 null** 이다.
→ 화면에 멀쩡히 떠 있는 모달까지 전부 유령으로 몰아 0.5초마다 강제로 닫았다.

**영향받던 것:** 캐릭터 선택 · 상점 · 장비 · 안전수첩 · 저장/불러오기 ·
일시정지 · 종료 · 엔딩 · 결과 리포트 — **모달 11종 전부**.
실제로 「시작하기」를 눌러도 캐릭터 선택 창이 사라져 **게임을 시작할 수 없었다.**

→ 계산된 스타일과 실제 사각형으로만 판정하도록 수정.
   내용 상자(`.bd-modal-box`)가 비어 있을 때만 진짜 유령으로 본다.

### 2-2. 같은 원인(offsetParent)의 다른 오판
| 위치 | 증상 |
|---|---|
| 관측 API `vis()` | 캐릭터 선택·증강·장소 카드가 검수에 전혀 안 잡힘 |
| ESC 처리 `openPanel()` | 열린 패널을 한 번도 못 찾아 ESC가 패널 위에 일시정지를 겹쳐 열던 문제 |

### 2-3. ESC로 창을 닫으면 «움직일 수 없게» 되던 문제
v72의 ESC 처리는 클래스만 벗겼다. 게임 내부 상태(`shopOpen`·`invOpen`)는 열린 채라
화면은 닫혔는데 이동이 막히고, 잠시 뒤 창이 다시 열렸다.
→ `closeShop()`·`closeInventory()` 등 **정식 닫기 함수를 호출**하도록 변경.

### 2-4. 투명벽 (콜라이더가 본체에서 떨어져 나감)
전수 스캔 결과 실제 사용 맵에서 2건:
| 맵 | 오브젝트 | 어긋남 |
|---|---|---|
| 212 와우리 | 쌓여있던 위험들(최종 보스) | x로 **0.46** |
| 213 상리 | 어두운 산책로 | y로 **0.14** |

→ `bd-core-fix-v147` 이 부팅 시·주기적으로 **콜라이더가 본체와 전혀 겹치지 않으면
자동으로 본체 위치로 되돌린다.** (일부러 꺼 둔 콜라이더 `cw/ch=0` 은 건드리지 않음)

### 2-5. 실내(문화의집)에 들어가면 길안내가 사라지던 문제
`BD_currentGuide()` 는 실내에서 항상 null, 엘리베이터 안내는 프롤로그 단계에서만 켜졌다.
회복하러 문화의집에 들어가면 **나가는 길을 아무도 알려 주지 않았다.**
→ `bd-exit-guide-v147` — 실내에서는 계속 「밖으로 나가기(엘리베이터)」를 가리킨다.
   (프롤로그 중에는 기존 안내가 우선)

### 2-6. 한 리를 끝내면 다음 리로 가는 길을 알 수 없던 문제
와우리 부탁을 다 해결해 2장(상리)이 시작되면 길안내가 **완전히 사라졌다.**
「도로 끝까지 걸어가면 옆 리로 넘어간다」는 규칙을 알 수 없어 그대로 멈춘다.
→ `bd-travel-guide-v147` — 진행 중인 장의 리가 다른 맵이면
   그 리로 이어지는 **도로 끝(관문)을 계속 가리키고**, 장이 바뀔 때 담이가 한 번 알려 준다.

### 2-7. 「F8을 눌러 본편 맵을 선택하세요」가 계속 뜨던 문제
길안내의 목적지 맵 표가 **구맵(1~5) 기준**으로 남아 있었다.
실제 무대인 4개 리 월드(210~213)에서는 언제나 「지금 맵은 임무 지역이 아님」으로 오판해,
개발용 단축키 안내만 반복했다.
→ `NAV_MAIN_STAGE` 를 212/213/211/210 으로 교정 + 문구를 플레이어 말로 교체.

### 2-8. 길안내 패널이 늘 「현재 임무 없음」이던 문제
`QUESTS`/`SUB_QUESTS`/`NPC_QUESTS` 가 다른 클로저 안의 `const` 라
길안내 모듈에서는 항상 undefined 였다.
→ 전역으로 노출된 `window.BD_*` 목록을 함께 보도록 수정.

### 2-9. 담당 주민이 없는 위험요소 (인수인계 §4)
「NPC 1명당 부탁 1개」 규칙 때문에 주민보다 위험요소가 많은 리에서
**아무도 부탁하지 않는 위험요소**가 남아 영원히 잠겼다.
- 와우리 «떠도는 담배 연기» / 동화리 «방치된 쓰레기» / 수영리 «어두운 골목»

→ **배정된 주민이 없는 위험요소는 부탁 없이도 직접 조사 가능**하도록 게이트 완화.
   (에셋·주민 추가 없이 정화율 100%가 가능해졌다)

### 2-10. 주민에게 F가 안 먹던 문제
대화 판정이 «주민 발밑 한 점»까지의 거리였다.
스프라이트 바로 옆·위에 붙어 있어도 F가 먹지 않아
「F 키로 말을 걸어요」 안내가 떠 있는데 아무 반응이 없었다.
→ **주민이 차지한 사각형까지의 거리**로 판정하도록 변경.

### 2-11. 저장/이어하기가 사실상 동작하지 않던 문제
저장 계열이 둘로 갈라져 있었다.
- `autoSave()` → `fantasyRPG_save.auto`
- 이어하기 슬롯 UI → `bongdam_guardian_slot_0..2`

그래서 한참 진행해도 타이틀의 「이어하기」에는 «슬롯 1·2·3 — 비어 있음»만 떴다.
게다가 슬롯 저장 데이터에 **맵·좌표가 아예 빠져 있어**, 불러와도 구맵(봉담 광장)에서 시작됐다.

→ ① 자동 저장을 슬롯 계열에도 함께 기록 + 30초 주기 자동 저장
   ② 이어하기 목록 맨 위에 「자동 저장」 줄 표시
   ③ 슬롯 저장/불러오기에 **맵·좌표·현재 HP** 포함

### 2-12. 패배(HP 0) 후 길을 잃던 문제
HP가 0이 되면 게임오버 대신 문화의집 3층에서 전액 회복해 이어 하도록 되어 있다(v212, 좋은 설계).
그런데 부활 지점이 실내라 **길안내가 사라지고**, 게다가 «직전 맵의 회복 시설»을
계속 가리켜 있지도 않은 곳으로 안내했다.
→ 맵이 바뀌면 이전 맵의 안내 목표를 버리고, 실내에서는 나가는 길을 가리킨다.
   (검증: 상리에서 패배 → 문화의집 부활 → 「밖으로 나가기」 → 와우리 → 상리 복귀 확인)

### 2-13. 회복 안내가 «건물 속»을 가리키던 문제
회복 안내는 건물 사각형을 그대로 목표로 삼아, 큰 건물은 화살표가 벽 한가운데를 가리켰다.
→ 아래쪽(길 쪽) 가장자리 앞을 가리키도록 보정.

### ⚠ 2-14b. 정화한 자리에서 **주민에게 보고할 수 없어 장이 끝나지 않던 문제**
F키의 «가장 가까운 대상 하나만 반응» 라우팅에서
- 위험요소는 **정화 여부를 보지 않고** 후보에 올랐고 (반경 0.10)
- 주민은 «발밑 한 점»에서 반경 0.053 으로만 겨뤘다

부탁을 준 주민은 **자기 위험요소 바로 옆에 서 있는 것이 설계**다(동화리 낙서 ↔ 재현).
그래서 정화를 마치고 보고하러 가면, **이미 사라진 위험요소가 F를 계속 가로채**
아무 반응도 없고 장이 영원히 끝나지 않았다.

→ ① 정화·소멸·잠금된 위험요소는 F 후보에서 제외
   ② 주민도 «사각형까지의 거리»(0.075)로 겨루도록 판정 통일
   (검증: 재현 위·아래·좌·우 4방향 모두에서 보고 대사 정상 출력)

### 2-14. 보스 컷신 중에 걸어 다닐 수 있던 문제
최종 보스 대사창은 화면 전체를 덮는 연출인데 입력 차단 판정에서 빠져 있었다.
→ `#bd-boss-dlg.on` 을 입력 차단 조건에 추가.

---

## 3. v147에서 고친 것 — 연출·대사

| 항목 | 내용 |
|---|---|
| 대화 끼어듦 | 튜토리얼 독백이 타이머로 발화되며 진행 중인 주민 대화를 덮어썼다 → 대화창이 닫힌 뒤로 미룸 |
| 지역 클리어 연출 | 1.2초→6.2초→9.2초→13.5초 고정 타이머라 앞 대사를 읽는 중에 덮어써졌다 → **순서 큐**로 변경 |
| 인물명 불일치 | 프롤로그가 「수진 선생님」, 실제 NPC는 「문화의집 선생님」 → 전부 「문화의집 선생님」으로 통일 |
| UI 겹침 | 가방·인벤토리 등 패널은 `#game-screen` 안에 있어 고정 HUD보다 아래에 그려졌다 → 패널이 열리면 필드 HUD를 숨김 |

---

## 3-2. v147에서 채운 것 — 기능

### 시설 방문이 기록되지 않던 문제 (연쇄 두 가지를 함께 해결)
장소 카드를 띄우는 경로에서만 방문을 기록해서, **휴식(F)·건물 진입으로 실제로 이용해도**
`visitedFacilityIds` · `readFacilityGuideIds` 에 아무것도 남지 않았다. 그 결과
- 완주해도 결과 리포트의 「방문한 시설」이 **0/9**
- v80의 «시설 안내 전부 열람 보상»(500G·회복약 3·간식 3·안전 포인트 40)이 **영영 발동하지 않음**
- 최종장 조기 개방 조건(스탬프 6개)도 성립 불가

→ 휴식 대사·건물 진입을 감지해 방문·안내 열람·활동 스탬프를 함께 기록한다.
   (검증: 문화의집 진입 즉시 `visited/guided/stamped` 모두 기록됨)

### 문화의집 안에서 체력이 천천히 회복
쉬어 가는 공간이라는 성격에 맞춰 실내에서는 1.5초마다 HP 1 회복, 가득 차면 안내.
(검증: 40 → 6초 후 44)

### 최종 보스 그림
전용 스프라이트가 없어 노란 **👑 이모지**로 대체되고 있었다.
「버려진 것들이 모여 생긴 덩어리」 설정에 맞춰 **캔버스로 직접 그려** 등록한다
(어두운 자주색 덩어리 3겹 + 삐져나온 조각 + 보라 오라 + 빛나는 눈, 256×256).
에셋이 도착하면 `BD_ASSETS.set('field.hazard.final_boss', <url>)` 로 교체하면 된다.

### 태블릿·터치 안내 복구
v82의 문구 치환기가 `offsetParent` 로 표시 여부를 판정해 **한 번도 실행되지 않았다.**
게다가 튜토리얼 카드는 W·A·S·D 를 각각 키캡 상자로 나눠 그려서 글자 단위 치환으로는 잡히지 않고,
게임 틱이 매 프레임 카드를 다시 그려 0.7초 간격 수정은 곧바로 원복됐다.
→ 표시 판정을 고치고, 카드 전체 문장을 rAF 로 덧씌운다.
   (검증: 터치 기기에서 「🕹️ 왼쪽 조이스틱으로 움직여 보세요 · 💬 화면을 탭하면 대사가 넘어가요」)

---

## 3-3. v147 마무리 — develop-web-game 스킬 규격 점검에서 추가로 잡은 것

| # | 내용 |
|---|---|
| 31 | **`advanceTime(ms)` 가 죽어 있었다** — 존재하지 않는 `__bdTick` 을 부르고 있어 프레임이 한 칸도 나아가지 않았다. `gameLoop` 를 직접 N프레임 돌리도록 복구 (30프레임 → 0.045 이동, 3회 동일 = 결정적) |
| 32 | 관측 JSON에 좌표계 설명(`coords`) 추가 |
| 33 | **PC에서 아무 곳이나 클릭하면 전체화면으로 튀던 문제** — 모바일 주소창 숨김 코드가 데스크톱에도 걸려 있었다. 터치 기기에서만 자동 요청 + **Shift+F / F11** 토글 추가 (스킬은 `f` 를 권하지만 이 게임에서 `f` 는 조사·대화 키라 쓸 수 없다) |
| 34 | **캐릭터 선택에서 ESC → 갇힘** — 타이틀도 게임도 아닌 빈 배경에 갇혀 새로고침 외엔 방법이 없었다. 확정하지 않고 닫히면 타이틀로 복귀 |
| 35 | 「문화의집 선생님**님**」 이중 존칭 |
| 36~38 | 구 레이어에 남아 있던 「수진 선생님」 사본 2곳 + 「안내데스크의 문화의집 선생님」 겹말 |
| 39 | 프롤로그 배웅 대사 화자가 「안내데스크 수진」 — 플레이어가 만난 적 없는 인물이라 맥락이 끊겼다 → 「문화의집 선생님」 |

### 검수 하니스 쪽 발견 (게임 수정 아님, 기록용)
전투의 **타이밍 링**은 시작 후 약 **539ms**(줄어드는 원이 기준 원과 겹치는 순간)에 한 번만 눌러야 한다.
연타하면 첫 입력이 곧바로 MISS 로 확정돼 **피해가 0** 이 된다.
자동 검수가 연타하고 있어 전투가 교착됐다 → 링 배율을 읽어 겹치는 순간 한 번만 누르도록 수정
(적 113 → 8, 전투 19초). 사람이 플레이할 때는 원래 의도대로 동작한다.

---

## 3-4. 미검증 구간 전수 스윕에서 잡은 것 (v147-40 ~ v147-46)

각 맵의 **모든 F 대상**(주민·상점·정류장·시설·PC존·노래방)에 대해
「접근 → F → 열리는가 → 닫히는가 → 다시 움직일 수 있는가」를 자동으로 훑어 찾았다.

| # | 내용 |
|---|---|
| 40 | **밴드부 4명 중 3명에게 말을 걸 수 없었다** — PC존 F 반경(0.11)이 밴드부가 서 있는 줄을 통째로 덮고 있었다. PC존·노래방 핸들러는 `window` 캡처라 주민 대화(`document` 캡처)보다 «먼저» 실행돼, F를 눌러도 게임/노래 선택창만 떴다. → 바로 옆(0.035)에 주민이 있으면 대화를 양보 |
| 41 | 같은 이유로 **드럼 태오**는 노래방 반경에 갇혀 있었다 |
| 42 | **상점·가방이 열려 있는데 «입력 차단» 판정이 false** 였다. 다른 레이어들이 «조작 가능»으로 오판하고, 자동 검수도 열린 창을 놓쳤다 → 전체 패널 11종을 판정에 포함 |
| 43·44·45 | **버스 목적지 창이 ESC로 닫히지 않았다.** 이 창만 «클릭»으로만 닫히는데, 게임의 다른 모든 창은 ESC로 닫히고 조작 안내도 키보드 기준이다. 창이 떠 있는 동안 주민 대화까지 막혀, 키보드로만 하던 플레이어는 그대로 멈춘다. → ESC 닫기 + 다른 레이어가 ESC를 먼저 삼켜도 닫히는 안전망 |
| 46 | 전투 안내 말풍선이 **「아래 버튼으로 행동을 선택하세요」 문구를 덮어** 글자가 겹쳐 보였다 |

### 이 스윕에서 «정상»으로 확인한 것
```
문화의집   밴드부 4명 대화 ✅ · PC존 아케이드(갤러그/DOOM/스네이크/INFERNO) ✅ · 노래방 ✅
           선택창 ESC·버튼 닫기 ✅ · 게임 종료 후 이동 복구 ✅
와우리·상리·동화리·수영리
           지역 상점 4곳 ✅ · 버스정류장 3곳(열기→ESC→사방 이동) ✅
           주민 전원 대화 ✅ · 주요시설 안내 카드 ✅
상점        구매 → 소지금 차감 → 가방 «바로 사용하기» → HP 회복 ✅ (초코 간식 60G, HP 40→80)
전투        정화 스티커(타이밍 링) ✅ · 배지 스킬 [E] 카드 메뉴(빈 상태 안내 포함) ✅
           아이템 [I] 패널 ✅ · 물러나기 [ESC] → 전투 종료 → 이동 복구 ✅
임무창      메인/전설/월드 임무 정상 · «만난 적 없는 사람의 부탁» 자동 수락 0건 ✅
```

---

## 3-5. 실플레이 피드백 반영 (v147-47 ~ v147-51)

사용자가 실제 플레이 스크린샷으로 보고한 4가지:

| # | 내용 |
|---|---|
| 47 | **전투 튜토리얼에서 담이 말풍선이 안 보이던 진짜 원인** — v120에서 «건너뛰기» 버튼을 담이 HUD의 innerHTML에서 뺐는데 바인딩 코드가 남아 있었다. `querySelector('#bd-dami-skip')`가 null → TypeError → 그 아래의 **«전투 중 말풍선 위치를 잡는 400ms 인터벌(bd-in-battle 토글)»이 영영 설치되지 않았다.** 전투 중에는 CSS가 `bd-in-battle` 없는 말풍선을 숨기므로, 대사가 «재생은 되는데 안 보이는» 상태가 됐다. → null-safe 바인딩으로 수정. (검증: 전투 중 말풍선 표시 + 액션바 위 배치 확인) |
| — | **조사/물러나기 선택창** — 새 상태에서 재현 시도: 상리에서 선택창 정상 표시(h=194) 확인. 부탁 미수락 위험요소는 안내 독백이 뜨는 것도 정상. 다만 «상태는 열려 있는데 상자가 사라진» 경우를 대비해 **0.6초 뒤 자동 재표시 안전망** 추가 (`bd-ui-polish-v147`) |
| 48~51 | **카메라 줌아웃** — 4개 리 맵은 «정수 논리 픽셀 배율 2»(`BD_DISTRICT_LOGICAL_PIXEL_SIZE`)가 VIEWPORT를 다시 계산해 `BD_VIEW_SCALE`을 무시하고 있었다. 배율을 **2 → 1**로 내려 2배 줌아웃 (정수 배율이라 픽셀은 또렷). 이제 아파트 단지·편의점 등 **건물 하나가 통째로 화면에 들어온다.** 문화의집(101)은 1.35 |
| 49 | **불필요 상시 UI 정리** — ① 상단 중앙 「◯◯리 · 도로 끝에서 인접 리로 자연 이동 · 주요시설 0/16」 배너(우측 패널과 중복) ② 좌하단 「🎛 UI 조정」(개발용) ③ 캔버스 우하단 「BUILD vNNN」(개발용) — 셋 다 숨김, 에디터가 열려 있을 때만 표시 |
| 52 | **부탁이 필요한(잠긴) 위험요소가 옆 주민의 F를 가로채던 문제** — 부탁을 줄 주민은 설계상 자기 위험요소 바로 옆에 서 있는데, 잠긴 위험요소가 주민보다 조금이라도 가까우면 F가 «주민 이야기를 먼저 들어보자» 독백만 무한 반복했다(동화리 재현↔낙서에서 실측). → 잠긴 위험요소는 판정 거리를 +0.05 물려 옆 주민이 이기게 |

---

## 3-6. 에디터 자유 편집 복구 (v147-53 ~ v147-56)

사용자가 에디터에서 직접 확인한 문제: 콜라이더가 스프라이트에 붙은 채
이동·테두리 리사이즈·가로세로 변경이 전부 안 되고, 위험요소는 비율 조절이 안 됨.

| # | 원인과 수정 |
|---|---|
| 53 | **콜라이더 편집이 1초 안에 원위치로 되돌아가던 진짜 원인** — district 런타임의 `rebuildRegistries`(900ms)가 모든 건물에 `syncLandmarkAnchors`를 돌려 **«앵커 비율 × 본체 사각형»으로 콜라이더를 다시 써 버렸다.** 계측 결과: 드래그 중에는 움직이다가 mouseup 직후 스냅백. → 에디터가 열려 있고 콜라이더가 앵커 예상값과 다르면 «손으로 만진 것»으로 보고 **앵커를 역산해 저장**. 본체 이동/리사이즈 시 콜라이더가 따라가는 기존 동작은 유지 |
| 54 | **위험요소 비율 조절이 화면에 반영되지 않던 원인** — 렌더가 렉트와 무관하게 `max(rw,rh)` 기준 **정사각형**으로 그렸다. → «현재 화면 크기»를 렉트에 1회 굽는 마이그레이션(`hzRectV147`, `bd-hazard-rect-v147` 레이어) 후 **렉트를 그대로 그린다.** 기존 배치 모습은 그대로, 이후 편집은 WYSIWYG |
| 55 | v147 투명벽 자동교정이 **에디터가 열려 있는 동안엔 쉬도록** — 일부러 밖으로 옮기는 편집을 되돌리면 안 된다 |
| 56 | 손으로 만진 콜라이더(`userColliderV147`)는 **플레이 중 자동교정에서도 제외** |

검증 (자동 마우스 드래그, 수정 후 2.5초 유지 확인 = 앵커 동기화 2회 통과):
```
건물 콜라이더 리사이즈  cw 0.4022→0.4273 · ch 0.1547→0.1889 · 본체 불변 ✅
건물 콜라이더 이동      cx 0.5727→0.7264 · 본체 불변 ✅
위험요소 가로만 늘리기   rw +0.184 · rh 변화 0 (비율 자유) + 화면에 즉시 반영 ✅
수정 후 무인 완주        프롤로그→엔딩 막힘 0 · 콘솔 오류 0 (run16)
```

---

## 3-7. 에디터 닫기 후 게임 멈춤 (v147-57 ~ v147-60)

증상: 에디터를 켰다 끄면 게임 화면으로 돌아오지 않고 멈춘다.
`__gameLoopChainAlive` 쓰기 지점에 스택 트랩을 걸어 원인을 특정했다:

```
ESC → window.BD_togglePause → openPause() → RAF 체인 사망
```

3중 원인이 겹친 결과였다:
1. **에디터의 ESC 닫기가 v51에서 Ctrl+Q로 바뀌며 사라져** ESC가 게임까지 내려갔고,
2. 게임의 일시정지가 열리며 **루프를 죽였는데**, 모달은 에디터 UI 뒤라
   **유령 모달 청소기가 «클래스만 벗겨» 조용히 닫아** `_bdPaused`+루프 정지 상태만 고착,
3. 이후 에디터를 닫아도 **아무도 루프를 되살리지 않았다.**

| # | 수정 |
|---|---|
| 57 | 에디터가 열려 있으면 `openPause()` 진입 금지 |
| 58 | 유령 일시정지 모달은 클래스 제거가 아니라 **`BD_resumeGame()`(정식 재개)** 로 닫는다 |
| 59 | **ESC = 에디터 닫기 복원** (window 캡처, 정식 토글 경로) + **루프 소생 감시자**: 게임 화면인데 일시정지·전투·게임오버·타이틀 어느 것도 아닌 채 루프가 1.2초 죽어 있으면 되살린다 |
| 60 | (57~59 검증 중 발견) v147-4로 살아난 v72 ESC 패널 닫기가 **같은 ESC로 방금 열린 일시정지 모달을 «패널»로 오인해 즉시 숨기던** 회귀 → 일시정지 모달 제외 |

검증: 에디터 닫기 3경로(ESC·버튼·Ctrl+E) 모두 루프 생존+이동 복구 ✅ ·
일반 ESC 일시정지 열기/계속하기/ESC 토글 닫기 ✅ · 콘솔 오류 0

---

## 4. 검증 결과 (v147)

### 무인 완주 (오토파일럿, 치트 없음)
```
프롤로그(문화의집 3층)
  → 와우리 1장   부탁 2건 해결
  → 상리   2장   부탁 3건 해결
  → 동화리 3장   부탁 2건 해결
  → 수영리 4장   부탁 2건 해결
  → 최종장       최종 보스 정화
  → 엔딩 → 결과 리포트
막힘 0건 · 콘솔 오류 0건
```
결과 리포트 실측: 정화 **11/13** · 안전 지식 11/12 · **방문한 시설 4/9** · 만난 주민 9명 · 안전 등급 Lv.3
- «방문한 시설»은 수정 전 **0/9** 였다. 오토파일럿은 길만 따라가므로 4곳이고, 직접 둘러보면 더 올라간다.
- 11번째 정화는 **와우리 「떠도는 담배 연기」** — 담당 주민이 없어 영원히 잠겨 있던 위험요소다.
  §2-9 게이트 완화가 실제 플레이에서 해결됨을 확인했다.

### 개별 항목
```
콘솔 오류                    0건 (전 시나리오)
모달 개폐 (7종)              전부 정상 (ESC 또는 닫기 버튼)
저장 → 새로고침 → 이어하기      정상 (맵·좌표·소지금 복원 확인)
패배(HP 0) → 부활 → 복귀       정상 (문화의집 → 「밖으로 나가기」 → 원래 리)
최종 보스 4방향 조사           전부 정상 (대사 6줄 → 선택창 → 전투)
4장 완료 → 최종장 개방          정상 (보스 잠금 해제 확인)
투명벽 스캔                   사용 맵(101/210~213) 잔여 0건
고아 위험요소                 조사 가능 (게이트 완화 확인)
해상도 1024/1366/1920         전부 정상 동작
```

---

## 5. 남은 과제 (우선순위 순)

1. **에셋 교체** — 최종 보스는 v147에서 **캔버스로 그린 임시 그림**을 쓰고 있다.
   정식 아트가 오면 `BD_ASSETS.set('field.hazard.final_boss', <dataURL 또는 경로>)` 로 교체.
   그 밖에 스킬 이펙트 6종, 시설 사진 9곳이 아직 없다.
2. **주민 추가** — 리별 주민이 2명뿐이라 부탁이 2개로 고정된다.
   (v147에서 «부탁 없는 위험요소도 조사 가능»하게 풀어 진행 막힘은 사라졌지만,
    이야기로서는 주민이 부탁해 주는 편이 자연스럽다)
3. **난이도** — 튜토리얼 전투 직후 HP가 100 → 50 근처까지 떨어지고,
   두 번째 전투에서 한 자릿수까지 내려간다.
   패배해도 문화의집에서 전액 회복해 이어 가고, 실내 회복·휴식 동선도 있어 막히지는 않지만
   초반 체감이 가파르다. 수업용이라면 튜토리얼 전투의 적 공격력을 한 단계 낮추는 것을 권한다.
4. **결과 리포트 «방문한 시설»** — v147에서 기록이 되기 시작했지만(0/9 → 4/9),
   9곳을 다 채우려면 각 리의 주요 시설을 직접 찾아가야 한다.
   수업 시간이 짧다면 시설 방문을 유도하는 서브 목표를 하나 넣는 것이 좋다.

### v147에서 처리 완료된 항목 (구 인수인계 §3)
- ✅ 투명벽 · NPC 대화 끼어듦 · 위험요소 조사 불가
- ✅ 시설 정보 보상 (v80 보상이 발동하지 못하던 원인 제거)
- ✅ 문화의집 HP 회복
- ✅ 재현 위치 (v80이 이미 낙서 벽 위로 옮기고 있음 — 보고 불가 원인은 F 라우팅이었다)
- ✅ 탭 전용 튜토리얼
- ✅ 아이템 표시 (상점 구매 → 가방·전투 아이템 반영 확인)
- ✅ `#bd-arcade-tip` · `#bd-karaoke-tip` 화면 밖 잔상 정리

---

## 6. 주의사항 (반드시 읽을 것)

### 코드 구조
- 기능이 **버전별 레이어(`<script id="bd-*-vNN">`)로 쌓여 있다.** 80개 이상.
- 같은 함수를 여러 레이어가 덮어쓰므로 **뒤에 오는 것이 이긴다.**
- 새 레이어 추가는 `</body>` 직전이 안전하다.

### 이미 겪은 함정
1. **`offsetParent` 로 표시 여부를 판정하지 말 것.**
   `position:fixed` 요소는 항상 null이다. → 이 한 가지가 v145~v146의 최대 버그였다.
   반드시 `getComputedStyle()` + `getBoundingClientRect()` 로 볼 것.
2. **패널을 닫을 때는 클래스만 벗기지 말 것.** 게임 내부 플래그가 남아 조작이 잠긴다.
3. **타이머로 대사를 띄우지 말 것.** 진행 중인 대화를 덮어쓴다. 큐에 넣고 비었을 때 내보낼 것.
4. 담이 대사는 대사창이 아니라 전용 말풍선(`#bd-dami-hud`)이다.
5. 담이는 «각성» 전까지 말하지 않는다 (`bd_dami_awake`).
6. 전투는 증강 3택을 고를 때까지 끝나지 않는다 — 정상 설계.
7. 맵 충돌은 기본 꺼져 있고(`BD_MAP_COLLISION`), 4개 리 월드는 `stage.collision=true` 로 켠다.

### 저장 키
```
fantasyRPG_save                      autoSave 계열 (auto)
bongdam_guardian_slot_0..2 / _auto   슬롯 저장 (v147부터 auto 포함)
bongdam_guardian_v160                진행 상태
bongdam_rpg_editor_data_v5_2_quest   에디터 배치 ← 오염되면 위험요소가 이상해짐
bd_battle_tutorial_seen              전투 튜토 실제 시청 여부
bd_dami_awake                        담이 각성
bd_tut2_done                         프롤로그 튜토리얼 완료
bd_hzquest_v57                       위험요소별 부탁 수락 상태
```

### v147에서 추가된 레이어
```
bd-core-fix-v147           투명벽 교정 + 조작 잠김 자가 복구 + 맵 전환 시 낡은 안내 폐기
bd-exit-guide-v147         실내 → 밖으로 나가기 안내
bd-travel-guide-v147       다음 리로 가는 도로 끝 안내
bd-panel-hud-v147          패널 열릴 때 필드 HUD 숨김
bd-autosave-continue-v147  자동 저장 ↔ 이어하기 연결
bd-facility-life-v147      시설 방문 기록 · 실내 회복 · 보스 그림 · 터치 안내 · 잔상 정리
```

### 최종 보스 그림 교체 방법
```js
// 콘솔에서 한 줄이면 교체된다 (localStorage에 저장돼 새로고침해도 유지)
BD_ASSETS.set('field.hazard.final_boss', '<정식 스프라이트 dataURL 또는 경로>');
```
지우고 기본(자동 생성 그림)으로 되돌리려면 `BD_ASSETS.remove('field.hazard.final_boss')`.
