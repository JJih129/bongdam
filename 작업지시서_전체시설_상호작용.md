# 작업지시서 — 전체 시설 상호작용 구현 (v148)

> 대상 파일: `봉담지킴이_v147.html` (라이브 월드 = [4개 리 월드], 스테이지 210~213)
> 목표: 현재 배경 랜드마크(구경 전용)로만 존재하는 **23개 시설 전부에 F 상호작용**을 구현한다.
> 원칙: 기존 스탬프 시설 12곳의 챕터 진행·수집률·세이브 데이터를 **절대 오염시키지 않는다.**

---

## 0. 현행 구조 요약 (코드 지도)

| 기능 | 위치 (검색 키워드) | 동작 |
|------|------|------|
| v24 월드 데이터 | `[4개 리 월드]` (한 줄 JSON, 이스케이프 문자열) | 각 landmark에 `facilityId / label / facilityCategory / facilitySummary / facilityActivity / majorFacility` 보유 |
| 시설 레지스트리 재구축 | `function rebuildRegistries()` | 900ms마다 STAGES 오브젝트의 플래그를 읽어 registry 재생성. **`majorFacility:false`면 상호작용 대상에서 제외** |
| F키 판정 | `function nearestInteractiveFacility()` | `majorFacility` && 거리 170px 이내만 반환 |
| F키 핸들러 | `function installInteraction()` | **capture phase**로 등록, 시설을 잡으면 `stopImmediatePropagation()` |
| 시설 모달 | `openFacility()` / `openSharedEntry()` / `buildModalShell()` | 카테고리·요약·주소 + "시설 이용 정보 확인" 버튼(`persistVisit`) |
| 라벨 렌더 | `function drawLabels()` | major=`◆/✓` 항상 표시, 비major=`·` 근접(220px)시만 |
| 기존 승격 패치 | `<script id="bd-facility-major-v39">` | `promote()` + `setInterval(tick, 600)` 패턴으로 런타임 승격 (이 패턴을 그대로 따라할 것) |
| 휴식(전체 회복) | `REST_RX` (`/문화의집|도서관|…|카페/`) + keydown 핸들러 | 라벨 정규식 매칭 건물 앞 F → 전체 회복 |
| 상점 | `injectShop()` / `shopObj()` (와우리 212 전용) + `openShop()` | 약국·편의점 계열 1곳을 상점화, `interactable:'shop'` |
| 시설 안내 카드 | `BD_showPlaceCard(fid, …)` | `BD_REGISTRY.FACILITY_DEFINITIONS` 키 기반 |

**핵심 설계**: 새 플래그 `minorFacility:true`를 도입한다.
`majorFacility:true`(상호작용 파이프라인 통과) + `minorFacility:true`(스탬프·수집률·챕터 로직 제외)의 조합으로 "상호작용은 되지만 진행도에는 안 잡히는 시설"을 만든다.

---

## 1. 승격 대상 목록 (23곳 — v24 데이터의 실제 facilityId)

액션 타입: `browse`=안내만 / `rest`=안내+휴식(전체회복) / `shop`=안내+휴식+상점

### 수영리 (스테이지 210)
| facilityId | 시설 | 액션 |
|---|---|---|
| `tongtoon` | 통툰 화성봉담점 (만화카페) | rest |
| `uri_home_mart` | 우리홈마트 수영점 | shop |

### 동화리 (스테이지 211)
| facilityId | 시설 | 액션 |
|---|---|---|
| `smile_forest` | 웃음만발놀이숲 | rest |
| `bongdam1_fountain` | 봉담1지구 분수광장 | rest |
| `clear_spring_cafe` | 카페 맑은샘 | rest |
| `the_holic_cafe` | 더홀릭보드게임카페 봉담점 | rest |
| `boardking_cafe` | 보드킹 보드게임카페 봉담점 | rest |
| `donghwa_pharmacy` | 동화약국 | shop |
| `jeongdaun_pharmacy` | 정다운약국 | shop |

### 와우리 (스테이지 212)
| facilityId | 시설 | 액션 |
|---|---|---|
| `wauri_culture_park` | 와우리문화공원 | rest |
| `dream_stationery` | 드림문구 | shop |
| `wawoo_pharmacy` | 와우약국 | shop ※기존 shopObj() 대상 — Task 5 참조 |
| `university_pharmacy` | 대학약국 | shop |

### 상리·외곽 (스테이지 213)
| facilityId | 시설 | 액션 |
|---|---|---|
| `coffee_book_nono` | 봉담커피앤북작은도서관·노노카페 | rest |
| `doran_doran_park` | 도란도란어린이공원 | rest |
| `bongdam_lake_park` | 봉담호수공원 | rest |
| `eoullim_park` | 어울림공원 | rest |
| `bongdam2_ecosports` | 봉담2 생태체육공원 | rest |
| `sambong_neighborhood_park` | 삼봉근린공원 | rest |
| `deulnyeok_oreum_park` | 들녘오름공원 | rest |
| `sangri_pharmacy` | 상리약국 | shop |
| `bongdam_admin_pharmacy` | 봉담행정약국 | shop |
| `alpha_stationery` | 알파문구 봉담점 | shop |

---

## 2. Task 1 — 신규 패치 스크립트 추가 `bd-facility-minor-v148`

`<script id="bd-facility-major-v39">` 블록의 **닫는 `</script>` 바로 아래**에 새 스크립트 블록을 추가한다. v39와 동일한 "런타임 재적용(tick + setInterval 600ms)" 패턴을 쓴다 — 900ms짜리 `rebuildRegistries`가 계속 돌기 때문에 1회 실행이 아니라 반복 적용이어야 안전하다 (플래그는 오브젝트 자체에 심으므로 실제로는 최초 1회 이후 no-op).

```html
<script id="bd-facility-minor-v148">
/* (v148) 전 시설 상호작용 — 배경 랜드마크 23곳을 '보조 시설(minorFacility)'로 승격.
   F 상호작용·시설 안내 모달은 열리지만, 스탬프·수집률·챕터 진행에는 잡히지 않는다. */
(function(){
  'use strict';
  // facilityId → 액션 타입 (rest: 휴식 제공 / shop: 휴식+상점 / browse: 안내만)
  var MINOR = {
    210: { tongtoon:'rest', uri_home_mart:'shop' },
    211: { smile_forest:'rest', bongdam1_fountain:'rest', clear_spring_cafe:'rest',
           the_holic_cafe:'rest', boardking_cafe:'rest',
           donghwa_pharmacy:'shop', jeongdaun_pharmacy:'shop' },
    212: { wauri_culture_park:'rest', dream_stationery:'shop',
           wawoo_pharmacy:'shop', university_pharmacy:'shop' },
    213: { coffee_book_nono:'rest', doran_doran_park:'rest', bongdam_lake_park:'rest',
           eoullim_park:'rest', bongdam2_ecosports:'rest', sambong_neighborhood_park:'rest',
           deulnyeok_oreum_park:'rest', sangri_pharmacy:'shop', bongdam_admin_pharmacy:'shop',
           alpha_stationery:'shop' }
  };
  window.BD_MINOR_FACILITY_ACTION = {};   // facilityId → 액션 (모달 분기에서 사용)

  function promoteMinor(o, action){
    if (!o) return;
    o.majorFacility = true;      // 기존 상호작용 파이프라인(F판정·모달·라벨)에 태운다
    o.minorFacility = true;      // 단, 수집률·스탬프에서는 제외 (Task 2)
    o.collectible = false;
    o.conceptFacility = true;
    o.conceptLandmark = false;
    o.visualOnly = false;
    if (o.hidden) o.hidden = false;
    if (o.locked) o.locked = false;
    window.BD_MINOR_FACILITY_ACTION[o.facilityId] = action;
  }

  function tick(){
    try{
      if (typeof STAGES === 'undefined') return;
      Object.keys(MINOR).forEach(function(sid){
        var st = STAGES[sid]; if (!st) return;
        var map = MINOR[sid];
        [(st.objects || []), (st.__v24Landmarks || [])].forEach(function(list){
          list.forEach(function(o){
            if (o && o.facilityId && map[o.facilityId]) promoteMinor(o, map[o.facilityId]);
          });
        });
      });
    }catch(e){}
  }
  tick();
  setInterval(tick, 600);
})();
</script>
```

**주의**: 대상 매칭은 반드시 `facilityId`로 한다. 라벨 매칭은 에디터에서 라벨을 수정하면 깨진다.

---

## 3. Task 2 — `rebuildRegistries()` 수정 (수집률 오염 방지)

`rebuildRegistries` 안의 landmark 처리 블록을 찾는다 (검색: `if (!landmark.majorFacility) return;`). 현재 코드:

```js
if (!landmark.majorFacility) return;
if (landmark.sharedEntryGroup && SHARED_OPTIONS[landmark.sharedEntryGroup]) {
  ...
} else {
  landmark.conceptFacility = true;
  landmark.collectible = true;                    // ← minor도 collectible이 돼 버림
  facilityRegistry[landmark.facilityId] = landmark;
  stage.facilityIds.push(landmark.facilityId);    // ← 수집률 분모에 들어가 버림
}
```

else 분기를 아래처럼 고친다:

```js
} else {
  landmark.conceptFacility = true;
  landmark.collectible = !landmark.minorFacility;   // (v148) minor는 수집 대상 아님
  facilityRegistry[landmark.facilityId] = landmark; // 모달·방문기록은 가능하게 등록 유지
  if (!landmark.minorFacility) stage.facilityIds.push(landmark.facilityId);  // (v148)
}
```

**검증 포인트**: `stage.facilityIds`를 참조하는 HUD·챕터 로직(검색: `facilityIds`, `majorFacilityEntries`)에서 minor가 세어지지 않는지 확인. `majorFacilityEntries: Object.keys(facilityRegistry).length` 같은 디버그 카운트는 표시용이면 그대로 둬도 된다.

---

## 4. Task 3 — `openFacility()` 모달에 액션 버튼 추가

`openFacility(landmark)` 함수(검색: `function openFacility`)를 수정한다. 현재는 "시설 이용 정보 확인" 버튼 하나뿐이다. minor 시설이면 액션 타입에 따라 버튼을 추가한다:

```js
function openFacility(landmark) {
  const facility = facilityRegistry[landmark.facilityId] || landmark;
  const options = buildModalShell(landmark, landmark.label,
    landmark.facilitySummary || landmark.facilityActivity);

  const action = landmark.minorFacility
    ? (window.BD_MINOR_FACILITY_ACTION || {})[landmark.facilityId] : null;

  if (!landmark.minorFacility) {
    // ── 기존 스탬프 시설: 기존 코드 그대로 ──
    const visit = document.createElement('button');
    visit.type = 'button';
    visit.textContent = '시설 이용 정보 확인 · ' + (landmark.facilityActivity || '방문 기록');
    visit.addEventListener('click', function () {
      persistVisit(facility); closeModal(); updateHud();
    });
    options.appendChild(visit);
  } else {
    // ── (v148) 보조 시설 ──
    if (action === 'rest' || action === 'shop') {
      const rest = document.createElement('button');
      rest.type = 'button';
      rest.textContent = '💗 잠시 쉬어 가기 (체력 회복)';
      rest.addEventListener('click', function () {
        closeModal();
        if (window.BD_minorRest) BD_minorRest(landmark);
      });
      options.appendChild(rest);
    }
    if (action === 'shop') {
      const buy = document.createElement('button');
      buy.type = 'button';
      buy.textContent = '🏪 물건 구경하기 (상점)';
      buy.addEventListener('click', function () {
        closeModal();
        if (window.BD_openShop) window.BD_openShop();
      });
      options.appendChild(buy);
    }
    const look = document.createElement('button');
    look.type = 'button';
    look.textContent = '👀 ' + (landmark.facilityActivity || '둘러보기');
    look.addEventListener('click', function () { closeModal(); });
    options.appendChild(look);
  }
  appendClose(options);
  window.setTimeout(function () {
    const b = options.querySelector('button'); if (b) b.focus();
  }, 0);
}
```

`BD_minorRest` 전역 함수는 Task 4에서 만든다.

**상점 함수 주의**: `openShop`은 파일에 스코프가 다른 두 개가 존재한다 (`#shop-overlay`를 여는 편의점 상점 / `window.BD_openShop = openShop`으로 노출된 것). **`window.BD_openShop()`이 편의점 상점 UI를 여는지 실제 실행으로 확인**하고, 아니면 편의점 쪽 `openShop`(검색: `document.getElementById('shop-overlay').classList.add('open')` 포함된 함수)을 `window.BD_openConvenienceShop = openShop;`으로 노출한 뒤 그걸 호출한다.

---

## 5. Task 4 — 휴식 전역 함수 + 기존 REST 핸들러와 충돌 정리

### 5-1. `window.BD_minorRest(landmark)` 신설

Task 1 스크립트 안(또는 별도 블록)에 추가. 기존 REST 핸들러(검색: `REST_RX =`)의 회복 연출을 재사용한 형태:

```js
window.BD_minorRest = function(landmark){
  try{
    var full = (typeof heroHP !== 'undefined') && heroHP >= maxHP();
    heroHP = maxHP();
    if (window.BD_syncHP) BD_syncHP(heroHP, false);
    if (window.HSR) HSR.sp = Math.max(HSR.sp || 0, 3);   // MP도 소량 회복 (기존 휴식과 동일 수준으로 조정)
    var label = String(landmark.label || '시설');
    var isPark = /공원|광장|놀이숲/.test(label);
    var isShop = /약국|마트|문구/.test(label);
    if (typeof showDialog === 'function'){
      showDialog('나', full
        ? ['(' + label + '에서 잠시 쉬었다. 몸도 마음도 가뿐해!)']
        : isPark ? ['(' + label + ' 벤치에서 바람을 쐬며 쉬었다.)', '(체력이 모두 회복됐다!)']
        : isShop ? ['(' + label + ' 앞 의자에서 잠깐 숨을 돌렸다.)', '(사장님이 물 한 잔을 건네주셨다 — 체력이 모두 회복됐다!)']
        : ['(' + label + '에서 휴식을 즐겼다!)', '(체력이 모두 회복됐다!)']);
      setTimeout(function(){ try{
        var ov = document.getElementById('dialogue-overlay');
        if (ov && ov.offsetHeight && !(window.HSR && HSR.active)) ov.click();
      }catch(e){} }, 6000);
    }
  }catch(e){}
};
```

### 5-2. 기존 REST 핸들러 이중 발동 방지

v24 F키 핸들러는 **capture phase + `stopImmediatePropagation()`**이므로, 승격된 시설 앞에서는 모달이 항상 먼저 열리고 기존 REST 핸들러(라벨 정규식 매칭 F 휴식)는 도달하지 못한다 → 원칙적으로 충돌 없음.

단, 다음 두 가지를 확인·수정한다:
1. **REST 핸들러가 먼저 등록된 경우**: capture 등록 순서에 따라 REST가 선점할 수 있다. REST 핸들러(검색: `var n = BD_nearestRest();`) 초입에 가드 추가:
   ```js
   // (v148) v24 시설 모달이 잡을 수 있는 시설이면 모달에 양보
   try{ if (typeof nearestInteractiveFacility === 'function') {} }catch(e){}
   var v24m = document.getElementById('bd-district-facility-modal');
   if (v24m && v24m.classList.contains('open')) return;
   ```
   (핸들러가 다른 IIFE 스코프라 `nearestInteractiveFacility`를 직접 못 부르면, v24 쪽에서 `window.BD_v24NearestFacility = nearestInteractiveFacility;`로 노출한 뒤 `if (window.BD_v24NearestFacility && BD_v24NearestFacility()) return;` 가드를 넣는 방식이 확실하다.)
2. **`REST_FALLBACK_RX`(약국·마트 대체 휴식)**: 약국들이 전부 모달을 갖게 되므로 fallback 휴식은 사실상 사장된다. 지우지 말고 그대로 둔다 (에디터로 minor 승격 대상을 지운 배치에서 여전히 안전망 역할).

---

## 6. Task 5 — 와우약국(기존 상점 지정 건물) 특례

와우리 212의 상점 시스템(검색: `function shopObj`)은 라벨 `/약국|편의점|슈퍼|마트/` 매칭 건물 중 **와우약국을 우선** 상점으로 쓰고, 별도 setInterval이 그 오브젝트에 `interactable:'shop'`을 심는다.

- 와우약국이 minor 승격되면 F에서 **v24 모달이 먼저** 열린다. 모달의 "🏪 물건 구경하기" 버튼이 상점을 열므로 기능 자체는 유지되지만, **기존 튜토리얼("상점 열기" 안내)이 F 즉시 상점 열림을 전제**하는지 확인할 것 (검색: `bd_dami_tutorial_done`, `maybeStart`).
- 확인 결과 튜토리얼이 깨지면: 와우약국(`wawoo_pharmacy`)만 MINOR 목록에서 빼고 기존 shop 경로를 유지한다(가장 안전). 이 경우에도 나머지 22곳은 영향 없다.

---

## 7. Task 6 — 라벨 표시 조정 (`drawLabels`)

승격하면 라벨이 `◆`(major 스타일)로 항상 표시되어 화면이 어지러워진다. `drawLabels` 안 두 곳을 수정한다 (검색: `landmark.majorFacility ? (visited ? '✓ ' : '◆ ') : '· '`):

```js
// 표시 조건: minor는 기존 배경 랜드마크처럼 근접시에만
if ((!landmark.majorFacility || landmark.minorFacility)
    && !nearby && !editorEnabled && !window.BD_DISTRICT_SHOW_ALL_LABELS) return;
// 프리픽스: minor는 ◇ (상호작용 가능 표시는 하되 스탬프 시설과 구분)
const prefix = landmark.minorFacility ? '◇ '
  : landmark.majorFacility ? (visited ? '✓ ' : '◆ ') : '· ';
```

배경색·테두리색 삼항식(같은 함수 하단)도 원하면 minor용 중간톤을 추가한다 (선택).

---

## 8. Task 7 — 세이브·에디터 호환 확인

1. **세이브 직렬화**: `minorFacility` 플래그는 STAGES 오브젝트에 심기므로 세이브에 같이 저장될 수 있다. 구버전 세이브(플래그 없음)를 로드해도 Task 1의 tick이 600ms 내 재승격하므로 마이그레이션 불필요 — 이 동작을 실제 구세이브 로드로 확인할 것.
2. **에디터 툼스톤**: 에디터에서 삭제된 시설(`deletedSysIds`)은 tick이 다시 만들지 않는다 (오브젝트가 없으면 no-op). v39와 동일 — 확인만.
3. **릴리스 빌드**: `__BD_RELEASE_BUILD` 분기는 에디터 UI만 숨기므로 영향 없음 — 확인만.

---

## 9. QA 체크리스트

- [ ] 4개 리 각각에서 승격 시설 앞 F → 모달 열림 (23곳 전수: §1 표 순서대로)
- [ ] 공원(rest)에서 "쉬어 가기" → HP 전액 회복 + 독백 연출 + 6초 후 자동 닫힘
- [ ] 약국·문구·마트(shop)에서 "물건 구경하기" → 편의점 상점 UI 열림, 구매 정상
- [ ] 기존 스탬프 시설 12곳: 모달·방문 스탬프·챕터 진행 **변화 없음** (특히 수영약국 = major 유지)
- [ ] HUD 방문 수/수집률 분모가 늘지 않음 (Task 2 검증)
- [ ] 시설 근처에 위험요소·주민이 더 가까울 때 F가 그쪽에 양보되는지 (기존 v85 양보 로직 확인)
- [ ] 와우리 첫 진입 튜토리얼(첫 정화 → 상점 안내) 정상 진행 (Task 5)
- [ ] HP 0 → 문화의집 복귀 흐름 정상 (승격과 무관해야 함)
- [ ] 구버전 세이브 로드 → 600ms 내 minor 승격 재적용
- [ ] 에디터에서 minor 시설 이동/삭제 → 즉시 반영, 재주입 없음
- [ ] 라벨: minor는 근접시에만 `◇`로 표시, 화면 클러터 없음
- [ ] `검수도구/s_fac.js` 통과 (시설 방문 기록·실내 회복 회귀 확인)

---

## 10. 작업 순서 요약

1. Task 1 스크립트 블록 추가 (승격) → 이 시점에 F 모달까지는 동작
2. Task 2 rebuildRegistries 가드 (수집률 보호) — **Task 1과 같은 커밋에 반드시 포함**
3. Task 3 모달 버튼 분기
4. Task 4 휴식 함수 + REST 충돌 가드
5. Task 5 와우약국 튜토리얼 확인 (필요시 예외 처리)
6. Task 6 라벨 조정
7. Task 7 호환 확인 → QA 체크리스트 전수
