# QA 라운드 32 — 계측 기반 개선 + 완성도 QA (2026-08-18)

작업 방식: src/ 편집 → bundle → 데몬 프로브(bd.js) → 완주 런 → release.js. 아래는 발견·조치 목록(반복 갱신).

## A. 계측으로 확인된 사실 (v367b 기준)
- 실브라우저(144Hz 모니터) 전 장면 144fps, 헤드리스 60fps, 롱태스크 0. JS는 전체 CPU의 ≈8%(gameLoop self 1.5%) — 나머지는 캔버스 페인트(실기기 GPU).
- CPU x4(중급 모바일) 80fps, x6(저가) 41~44fps p95 38ms → 프레임당 CPU ≈3.5ms.
- 전투 반복 후 DOM 2468로 수렴(1회성 UI), 누수 징후 없음. setInterval 감시자 177개는 5초에 2ms — 무시 가능.
- **gameLoop가 144Hz에서 144회/초 실행** → 프레임 기반 타이머(대시 쿨다운·잔상·HW스킬) 2.4배 가속, 이동 20% 과속(실측 dx 0.325→0.390).

## B. 조치 (v368)
| # | 항목 | 조치 | 검증 |
|---|---|---|---|
| 1 | 고주사율 과속/타이머 가속 | gameLoop RAF 체인에 60fps 누적기 상한(0016), v332 dt 보정을 로직 프레임 간격 기준으로(0196) | 144Hz 이동 dx 0.3904→0.3252(=60Hz), gameLoop 63/s |
| 2 | 편집기 폴러 4개 매 프레임 실행(게시 모드 낭비) | 편집기 OFF 시 250ms 폴링(0030·0035·0037) | RAF 목록에 스케줄러+dt tick만 잔존 |
| 3 | 방문한 도서관 문 앞 F가 옆 드림문구를 엶(회복 안내대로 쉴 수 없음) | 지정 상호작용 지점이 반경 안이면 «방문 후순위(+500)» 면제 + 25px 우대(0099) | (0.219,0.319)에서 방문 여부 무관 도서관 모달+휴식 버튼 |
| 4 | 지도 ✅/⚠ 마커가 건물 가림(QA ISSUE-05) | 마커 핀 앵커+축소, 방문 시설은 외곽 글로우+✓ 배지(0207·0209·0226) | 스크린샷 map_v368 |
| 5 | (도구) 자동 bundle 훅 간헐 미발화 | 데몬 open/reload 시 src가 새로우면 자동 bundle, release.js 일치검사 | — |

## B-2. 데이터 무결성 유닛 테스트 (tools/unit_data.js, 릴리스 파이프라인 편입)
- 4개 리 36개 시설: 지정 상호작용 지점 존재 · 반경 안 «설 자리» 존재 · 문 앞에서 F가 그 시설을 고름(미방문/전부방문 양쪽) — **164/164 통과**.
- 위험요소 hazardId 유일, QUESTS 6개 참조 유효. 부탁 짝 없는 위험요소 5개는 선택 위험요소(bdOptional)로 정상.

## B-3. 완주 런(v368, 63분) — 4장 수영리 210↔211 루프 → 원인 확정·수정
- 원인: **하네스 경로 플래너 결함**. 골목 목표의 «가장 가까운 도달 칸»이 서/동 동률 → 동쪽 포켓 선택 시 우측 가장자리 차선(x≥0.972, y≈0.5 = 동화리 게이트 밴드)을 경유 → 지역 전환. 게임 데이터는 정상(도로 폭 0.90~0.98, 실플레이어는 가장자리 0.972 이상을 밟지 않는 한 전환 없음).
- 조치: path.js — 목표가 가장자리 밴드가 아니면 밴드(0.036) 칸을 통행 금지. 재현 스냅샷(ch4_loop)에서 골목 정화→4장 완료→최종장 진행 확인.
- **부수 발견(진짜 게임 버그, 유저 제보와 일치)**: 이어하기(페이지 재로드) 시 구 컨셉맵 런타임(0094)이 방문 시설 목록을 자기 등록부 ID만 남기고 삭제 → 신맵 시설 방문 33개→6개 소실, 지도 % 폭락, 다음 리 개방 조건 재요구. → 필터 제거(v368). 추가로 «장 완료→다음 리 개방(1.6초 지연)» 사이 저장 끊김을 부팅 후 1회 화해(BD_Chapter.check 멱등화)로 복구.
- 하네스: 스냅샷 복원을 페이지 스크립트 실행 전 주입으로 변경(부팅 후 덮어쓰면 메모리 상태가 재저장돼 스냅샷을 지움).

## C. 열린 항목 / 다음 후보
- 회복 안내(v78)가 폭 넓은 건물을 가리킬 때 화살표가 «지정 지점(문)»을 가리키는지 점검.
- 오토파일럿: 시설 목표는 지정 지점 조준(auto.js v368 반영) — 완주 런 재검증 중.
- 게임성 B/C: 수영리 보강, 장별 서사 훅, 상점 품목 풀, 전투 2~3턴 컷, 서브퀘 노출.
- 에셋 다이어트(미참조 에셋 제거·PNG→WebP), 유닛 테스트 계층.

## D. 진행 막힘 전용 테스트 (v368b~d)
- 완주 런 2회 연속 cleared(18.8분·18.7분) · 체크포인트 이어하기 완주 ch2(14.6분)/ch3(9.5분)/ch4(4.2분) · 회귀: 엣지 7/7·러시·어뷰즈·타이틀·부팅·스탬프·오염시작 통과 · 시작 캐릭터 2/3 첫 전투 OK.
- **터치 프롤로그 차단 발견·수정(v368d)**: «문화의집 선생님»(주민)이 v78 휴식 시설 정규식(문화의집)에 걸려 대화 반경 살짝 밖 F(터치 조사 버튼)에 «선생님에서 잠시 쉬었다»가 뜨고 배지를 못 받음 → 주민·위험요소는 휴식 대상 제외. 터치로 조이스틱→선생님 대화→배지→가방 ✕→엘리베이터→212 진출 확인(s_touchprog/s_touchbag).
- 모바일 세로(390×844)는 «기기 회전» 오버레이가 입력을 막음(의도). 가로 HUD 정상.
