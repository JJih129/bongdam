# v397 → v400 변경 정리 (2026-09-05 스냅샷 → 2026-09-13 현재)

- 기준: `봉담지킴이_게시용_v397.html`(BUILD `bd-bake-4f6024a0`, 커밋 66900c7 «개발 소스 스냅샷 v397», 9/05) ↔ 현재 source 브랜치(v400, 9/13).
- 규모: 커밋 63개 · 블록 150 → 172(새 블록 24, 제거 2 — 편집기 좌표 마커·구 지역 상점) · 검증 스크립트 8개 + 스모크·캡처 비교 추가.
- 가장 큰 차이 한 줄: **v397 배포본은 4개 리를 끝내도 최종 보스가 생기지 않아 엔딩에 갈 수 없었다** → 현재는 타이틀→엔딩 무인 완주 런 5회 연속 성공(24~32분).

## 1. 모바일·기기 대응 (v398, 9/05~9/06)
- 폰 / 태블릿 / PC 세 환경 완전 분리(bd-mobile-mode) — 모바일이면 전부 모바일 UI, PC면 전부 PC UI. 모바일 UI 글자·패널 3단계로 통일, 쌓임 순서 정리.
- 모바일 이식: 카메라(bd-mobile-camera)·대사창(bd-mobile-vn)·안전지도(bd-mobile-map)·가방(bd-mobile-inv)·UI 배율(bd-mobile-uiscale)·작은 화면 맞춤(bd-smallscreen-fit)·전체화면 복원(bd-fs-restore).
- 탭 이동(bd-tap-travel): 멀리 있는 대상을 누르면 그 앞까지 걸어감 · 탭 보정·확대(bd-tap-expand) · 플로팅 조이스틱 복구(웹판 STRIP 오등록).
- 갇히는 모달 탈출 보장(bd-modal-escape), 키보드 문구 → 터치 문구 치환(bd-mobile-guide), 세로 화면 안내·회전 잠금 힌트·시작 모달 수납(bd-mobile-polish), 12px 미만 글자 21→3개.
- 성능: 런타임 픽셀 예산으로 폰 39→60fps·jank 126→0, 실기기 계측 HUD(bd-perf-hud).

## 2. 로딩·용량·배포
- 초기 요청 128→5, 전송 4.5MB→606KB(로딩 최적화) · JS 최소화·외부 분리(index.html 2.7MB→272KB, brotli 52KB) · 건물 PNG 45장→WebP(-5.5MB) · 죽은 배경·폰트 제거.
- 서비스워커: 문서는 네트워크 우선(배포해도 옛 버전이 남던 문제) · PWA · 조기 로딩 화면.
- 배포 채널을 GitHub Pages 하나로(main 브랜치 직접 서빙, publish.js), Netlify·태그 워크플로 제거. 배포는 요청 시에만.

## 3. 에디터 배치 ↔ 게임
- 9/06 저장본을 게임에 반영(B안 전체 맞춤) · 위험요소가 ⚠️ 마커로만 보이던 문제(조회 경로 누락·지연 로딩 교착) 수정.
- **v400: 빌드 때마다 에디터 JSON → `0071 배치 데이터` 자동 생성**(gen_placement.js). 4개 리 주민 좌표·부탁 짝(hzTarget)·위험요소·보스 위치·삭제 툼스톤이 에디터 저장본 그대로 웹판에 들어간다(대사·에셋은 코드 정본). 상리 깨진 유리를 벽 안에서 공원 위 길로 이동(1c).

## 4. PC(마우스·키보드) 웹 26건 (bd-pc-polish, v399b)
- 대화창 클릭 진행, ESC 모달 스택, 장소 카드 대기, PC 자동 전체화면 끔, 키 안내 바 상시(J/M/Esc), 상단 가방·임무 버튼, hover 커서·툴팁, 대상 클릭→F, 클릭 이동, 임무 HUD 패널, 물러나기 2회 확인, 가방 탭·업적·안전도 가독성, 설정 통합(UI 크기·길안내·튜토 다시 보기·조작법), 타이틀 완주 표식.
- 진짜 원인 수정: 상점 위에 조사 선택창이 뜨던 문제(0226 자가치유가 시설 모달을 반응으로 인식 못 함).

## 5. 스토리·텍스트 (v399c~v399e)
- 주민 13명 첫 대사 원문 복구(JSON·베이크에 «완료 대사»가 굳어 있었음) — **웹판에 박 반장·순임 할머니·영자·준호·재이·재현이 처음 등장**.
- 스토리 개선안 S1~S6 · A1~A20 · B8·B9 구현: 스토리가 지목한 인물(서연·재현·은지 어머니·재이)에게 부탁 고정 짝, 임무 문구 «부탁 n/m · 시설 n/m», 지도 % 토스트, 지도 채우기 안내(bd-map-fill-guide), 사서 도현 = 지도 담당자(📖), 파티 합류→«연락처 교환», 엔딩 친구·선생님 대사, 3층 PC존·노래방은 완주 후 개방.
- 텍스트: 프롤로그 선생님 9→7줄, 담이 1인칭 복선, 시설 안내 63건 해요체, 용어 통일(봉담 활동 배지·봉담와우도서관·안전 지도·배지 스킬·체력·가게·숨은 업적), 달성 불가 업적 제외, 밴드부 4인 제거.

## 6. 치명·진행 막힘 버그
- **웹판 최종 보스 미생성**(`window.STAGES` 가 늘 undefined → 보스 정리 코드가 한 번도 안 돌음) → 엔딩 불가. 수정 후 완주 런 성공.
- 동화리 생태공원이 남쪽 도로 위에 놓여 관문 진입 시 갇힘 · 남쪽 관문의 와우리 진입 지점이 벽 안 · 은지가 약국 문 위에 서서 방문 불가 · 가게·지도 튜토 중 전투 버튼 막힘(bd-tuto-battle-yield) · 엔딩 위에 회복 안내·시설 창이 남음(bd-ending-guard) · 히든 카드 조기 지급 · 안전지도 튜토 라벨 불일치.

## 7. UI 검수 반영 (갤러리 86건 중 ~60건, bd-ui-polish2·bd-review-batch7 + 소스 20여 곳)
- 필드: 캔버스 «[F] 조사/대화» 중복 제거, 간판이 HUD 뒤로 숨지 않게, 키 안내 바 접기(«? 조작키» 칩), 모달 열림 중 담이 흐림, 미니맵 «시설 n/m», 길안내 배지 전 문구·나침반 칸 문구.
- 창: 가방 제목 «🎒 가방»·탭 안내·안전도 크림 톤·닫기 안내 대비, 수첩 카드 글자(자동 다크 톤 오작동)·블러·라벨, 임무 창 제목 «📋 임무»·보상 아이콘, 리포트 타일 3열, 장비 «🔧 장비», 지도 범례 ✓·🔒, 타이틀 «이어하기» 비활성 표시, 설정 «다시 보기»·조작법 겹침.
- 전투: 정화 스티커 카드 ✨ 구분, 스킬 메뉴 중 툴팁·«내 차례» 숨김, 플레이어 패널 «지킴이» 중복 제거, 경험치 «n / m», 전투 진입 시 필드 말풍선 정리, 툴팁 띄어쓰기.
- 모바일: 메뉴 버튼·HP 분리, 지도 머리 줄바꿈, F 안내 치환.

## 8. 구조 정리
- 툼스톤 블록·죽은 폴링 루프·구 옵션 모달·Z 키·맵 시안 DOM 제거, `BD_hazardInteract` 5겹·`BD_DAMI.show` 3겹 래퍼를 순서가 명시된 체인 블록(bd-hazard-chain·bd-dami-chain)으로 통합.
- 사장 코드 제거: 0056 블록 삭제, 구 심부름 NPC_QUESTS 7건, 무효 진행 호출 6곳, hideLegacySubs 폴링, stampTick.

## 9. 검증 체계 (v397 에는 없던 것)
- `qa.cjs all`(단일 진입점) · Playwright 스위트 8개(s_story_s 20 · s_pcfix 26 · s_wp5 11 · s_reviewfix 6 · s_batch6 15 · s_placement 18 · s_polish3 5 · s_batch7 11) · `smoke.cjs` 일괄.
- 무인 완주 런(drive.js + auto.js — 스킬·지도 튜토를 실제로 따라감): v399h 27.6분 → v400a 24.2분, 5회 연속 타이틀→엔딩 성공.
- UI 캡처 63장 갤러리(검수 메모 86건, 반영 표시) · 캡처 픽셀 비교(shots_diff) · 개선 루프 프롬프트(`봉담_개선루프.md`).

## 아직 안 한 것 / 결정 대기
- 갤러리 검수 메모 미반영 ~30건(창 5종 닫기·제목 통일, 안전지도 리 이름·% 칩 위치·마커 색, 모바일 타이틀 아트 겹침), 구조 정리 2차, 스토리 B(교훈 중복·감사 시퀀스), 수업 모드.
- 결정 대기: docx 대본 이식, 3지선다 부활, 장 완료 기준 완화, 배포, 에디터에서 삭제된 동화리 정류장.
