# 분석_코드.md — 코드 위생 점검 (v294 기준, 2026-08-12)

## 요약
- **문법 오류 0** (최근 30개 레이어 `node --check` 전부 통과)
- **콘솔 오류 0** (전체 실플레이·화면 전수 검수 중 pageerror/console.error 0건)
- 스크립트 블록 146개 (이름 있는 레이어 96개) — «뒤가 이긴다» 레이어 규약 유지
- 함수 감싸기(wrap) 패턴 11곳 — `__bdXxx` 플래그로 이중 감싸기 방지 확인됨 (BD_drawNpcQuestMarks.__v289/.__v293 등)

## 중복 정의 검사
`function NAME` 3회 이상 재정의 상위 목록은 전부 **IIFE 내부 지역 함수**(init/toast/tick/el/clamp 등)로
실제 전역 충돌이 아님. `window.X =` 재정의는 의도된 레이어 덮어쓰기(마지막 레이어 승리)로 동작.

## 발견·조치한 위생 문제
| 문제 | 위치 | 조치 |
|---|---|---|
| 에디터 전체 노출 (`var dev = true`) | 게시게이트 레이어 | v295: `?dev=1`일 때만 개발 UI |
| 「맵 시안」 버튼·패널 무게이트 | bd-concept-map-runtime | v298: 게시 모드 숨김 CSS |
| F 상호작용 «양보 교착» — 주민(≤65px 요구) ↔ 시설(점 85px) 서로 미룸 | 032 주민 핸들러 ↔ 067 시설 핸들러 | v295~v297: 시설=사각형 거리 110px, 주민 한계 65→120px, **양쪽 동일 비교식**(objD≤120 && objD<facD)으로 통일 — 정확히 한 핸들러만 반응 |

## 비고
- 우측 화면 가장자리의 붉은 세로 표식은 dev UI가 아니라 **잠긴 방향(공사중) 차단 표식**의 월드 렌더로 확인.
- FPS 측정은 4단계(연출)에서 requestAnimationFrame 샘플링으로 수행 예정.
