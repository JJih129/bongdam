# 분석_UI점검.md — 화면 전수 점검 (v294→v299 반영, 2026-08-12)

검수 방법: Playwright로 화면별 스크린샷을 찍어 직접 열람 (shots_scr_pc / shots_scr_pc2 / shots_scr_tab / shots_scr_port).
해상도: PC 1280×800 · 태블릿 1024×768 · 세로 820×1180(터치 UA).

## 화면별 결과
| 화면 | 상태 | 비고 |
|---|---|---|
| 타이틀 | ✔ | |
| 캐릭터 선택·시작 설정 | ✔ | |
| 필드(와우리) | ✔ | HUD·미니맵·리 진행 위젯·키바 정상 |
| 상단 패널 4종 (안전수첩·장비·장소수첩·안전지도) | ✔ | 클릭·표시 정상. 「건물」 버튼은 빌드에 없음(구명세) — 점검 목록만 갱신 |
| 임무창(J) | ✔ | 보상 아이콘 «카드» 픽셀화만 경미 |
| 가방(E)·지도(M)·설정·일시정지 | ✔ | |
| 시설 선택 카드(약국) | ✔ | 잠시 쉬어 가기/상점/설명 3버튼 |
| 상점 | ✔ | |
| 전투 진입·플레이어 턴·미니게임(링·리듬) | ✔ | r9·r8 라운드에서 검증 |
| 안전지도 첫 열람 안내 | ✔ | |
| 엔딩 모달 | ✔ | 본문 위 선택 패널 z-겹침은 «두 창 동시 강제 오픈» 검수 아티팩트 — 실플레이 경로에선 동시에 안 뜸 |
| 세로(820×1180) | ✔(설계) | «기기를 가로로 돌려주세요» 회전 안내 오버레이가 정상 표시 — 세로는 의도적 차단 |

## 발견 → 수정
1. **에디터 UI 전면 노출** (✂ 에디터 버튼·스테이지 탭) — `var dev = true` → v295에서 `?dev=1` 게이트 복원.
2. **「맵 시안」 개발 버튼** 상단 우측 노출·상단바와 겹침 — v298에서 게시 모드 숨김.
3. **동일 힌트 토스트 2중 표시** (상단 bd-toast + 하단 bge-toast) — 하단은 에디터 토스트로, dev 게이트 복원(v295)과 함께 소멸 확인.
4. 우측 가장자리 붉은 세로 표식 — dev UI가 아니라 잠긴 방향(공사중) 차단 표식의 정상 월드 렌더로 판명. 유지.

## 3단계 실측 결과 (가로 태블릿 1180×820 · 터치 UA)
- **터치 문구 분기: 이미 구현돼 있음** — `bd-touch-mode`에서 조이스틱·«조사 버튼»·«화면 탭» 기준 안내로 자동 전환 확인
- 위험요소 선택지: **실제 탭으로 확정 정상** (합성 클릭 실패는 검수 도구 한계였음)
- 전투 버튼: 정화 스티커/배지 스킬/아이템 113~149px ✓ · «물러나기» 높이 40px(44 미만, 경미)
- **리듬 미니게임 터치**: v301 화살표 버튼(56px)으로 5정타 PERFECT → 오버레이 정상 제거 실측
- 라이트 미니게임: 터치 표기(v301) 적용, 포인터 드래그 동작
- 콘솔: 무해한 브라우저 경고 1건(스크롤 중 touchstart 취소 시도) 외 0

## 남은 관찰(4단계)
- 임무 보상 «카드» 아이콘 저해상 — CSS 라운드 처리 검토
- «물러나기» 터치 높이 40→44px 상향 검토
