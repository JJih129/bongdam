# 모바일 UI 대응 (v398)

`성능최적화_v398.md` 의 후속. 로딩이 아니라 **화면·입력** 쪽 작업 기록.

---

## 1. 화면 구조 — 왜 캔버스가 화면 크기와 따로 노는가

### 실측된 구조

```
body            width 1200px   zoom 0.65      ← JS 가 계산해 인라인으로 넣음
 └ #game-screen width 1200px   (offset 1200x554,  화면상 780x360)
    └ #game-canvas  width:100%/height:100%
         레이아웃(offset) 1200x554
         백버퍼(canvas.width) 2400x1108
         화면상(rect)   780x360
```

핵심은 **`transform` 이 아니라 CSS `zoom`** 을 쓴다는 점이다. 이게 헷갈리는 모든 것의 원인이다.

| 측정 API | zoom 반영 | 위 예시 값 |
|---|---|---|
| `offsetWidth` | ❌ 안 함 | **1200** |
| `getBoundingClientRect().width` | ✅ 함 | **780** |

유니티에 비유하면 **논리 해상도(Reference Resolution) 1200px 고정 + 화면에 맞춰 전체를 축소**하는 방식이다. 좌표를 전부 1200 기준으로 짤 수 있어 편하지만, 대가가 있다.

### 대가 — 백버퍼가 화면과 무관해진다

`0017_anon.js:4499` 의 계산:

```js
const dpr = Math.min((navigator.maxTouchPoints > 0 ? 1.5 : 2), Math.max(1, devicePixelRatio || 1));
const wantW = Math.round((canvas.offsetWidth  * dpr) / 2) * 2;   // ← offsetWidth = 1200 고정
```

`offsetWidth` 는 zoom 을 모르므로 **항상 1200** 이다. 따라서:

```
백버퍼 폭 = 1200 x dpr상한   →  터치 기기에서 항상 1800px 고정
```

화면이 크든 작든 **렌더 비용이 1.5M 픽셀로 고정**된다. 이게 두 방향으로 어긋난다.

| 상황 | 백버퍼 | 실제 화면 픽셀 | 결과 |
|---|---|---|---|
| 소형 폰 (zoom 0.55, dpr 2) | 1800 | 660x2 = 1320 | **1.36배 과잉** — 안 보이는 픽셀을 그림 |
| 고DPR 폰 (zoom 0.65, dpr 3) | 1800 | 780x3 = 2340 | **0.77배 부족** — 살짝 흐림 |
| 태블릿 가로 (zoom 0.97, dpr 2.4) | 1800 | 1167x2.4 = 2800 | **0.64배 부족** — 눈에 띄게 흐림 |

즉 **작은 폰에서는 낭비, 큰 태블릿에서는 흐림**이 동시에 일어난다. 갤럭시 탭에서 화질이 아쉽다면 이 때문이다.

### 고칠 방향 (아직 미적용)

실제 표시 픽셀을 기준으로 상·하한을 잡는다:

```js
const rect  = canvas.getBoundingClientRect();          // zoom 반영된 진짜 표시 크기
const shown = rect.width * (devicePixelRatio || 1);    // 화면에 찍히는 물리 픽셀
const wantW = Math.min(canvas.offsetWidth * dpr, shown * QUALITY);   // QUALITY: 저사양 0.75 등
```

**단, 손대기 전에 확인이 필요하다.** 논리 해상도 1200 이 타일 크기·충돌 판정·배치 데이터 좌표계에
묶여 있으면 백버퍼만 바꿔도 렌더가 어긋날 수 있다. 백버퍼 «상한만» 거는 것은 비교적 안전하지만,
논리 해상도 자체를 낮추는 것은 별개의 큰 작업이다.

---

## 2. 탭 영역 보정 — `blocks/0256_bd-tap-expand-v398.js`

### 문제 (780x360 가로 실측)

| 요소 | 크기 | |
|---|---|---|
| `#tc-btn-f` 터치 컨트롤 | **51x51** | ✅ 유일하게 정상 |
| `#bd-menu-btns` 안전수첩 | 75x**34** | ❌ |
| `#bd-bag-top` 🎒 | **34x34** | ❌ |
| `#bd-mb-toggle` ☰ | **34x34** | ❌ |
| `#bd-touch-mapbtn` 🗺 | **30x30** | ❌ |
| `#bd-settings-btn` ⚙️ | **25x25** | ❌ 최악 |
| `#bd-fullscreen-return` | 92x**23** | ❌ |

**보이는 클릭 요소 9개 중 8개가 44px 미만.** 타이틀 화면은 7개 중 7개 전부.
터치 컨트롤만 대응됐고 HUD 크롬 버튼은 데스크탑 크기 그대로다.

### 왜 «크기를 키우기»가 답이 아닌가

우상단 버튼들은 서로 **간격 0~14px** 로 밀집해 있다. 일괄 44px 로 키우면 서로 겹쳐
엉뚱한 버튼이 눌린다.

투명 `::after` 로 히트 영역만 넓히는 방법도 시도했으나 **스태킹에 좌우돼 실패**했다.
`#bd-mb-toggle` 의 `::after` 는 `#bd-title-frame` 아래로 깔려 무효였고,
z-index 를 올리면 이번엔 보이지 않는 사각형이 모달 위를 덮어 정상 탭을 삼킨다.

### 채택한 방식 — 빗나간 탭 전달

스태킹과 무관하게, **탭이 «아무것도 아닌 곳»에 떨어졌을 때만** 22px 안의 가장 가까운
작은 버튼으로 클릭을 전달한다. 실제 UI 를 눌렀을 때는 전혀 개입하지 않는다.

드래그 보호가 중요하다 — 이 게임은 화면을 누른 채 드래그해서 이동한다. 그래서
**이동 10px 미만 + 350ms 미만인 «탭»만** 보정한다.

검증 결과:

| 케이스 | 기대 | 결과 |
|---|---|---|
| 버튼 12px 밖 탭 | 전달 | ✅ 1회 |
| 40px 밖 (범위 초과) | 무시 | ✅ 0회 |
| 드래그 (이동 조작) | 무시 | ✅ 0회 |
| 직접 탭 | 중복 없음 | ✅ 0회 |

시각적 변화는 전혀 없다. QA 는 `BD_TAP.inspect()` 로 현재 보정 대상을 확인할 수 있다.

---

## 3. 실기기 성능 계측 — `blocks/0257_bd-perf-hud-v398.js`

데스크탑 에뮬레이션으로는 실제 GPU·발열·메모리를 재현할 수 없다. 특히 1번에서 본 대로
**화면 크기와 렌더 비용이 비례하지 않는** 구조라 실기기 수치가 반드시 필요하다.

```
<주소>?perf=1     ← 이렇게 접속하면 좌상단에 계측 HUD 가 뜬다
```

평소처럼 플레이한 뒤 **HUD 를 길게 누르면** 전체 보고서가 클립보드로 복사된다.

| 지표 | 의미 |
|---|---|
| FPS | 최근 1초 프레임 수 |
| **p95** | 느린 쪽 5% 프레임 시간. **평균 FPS 가 60이어도 이 값이 크면 «툭툭» 끊긴다** |
| jank | 33ms(30fps) 초과 / 100ms 초과(심각) 프레임 수 |
| Mpx | 캔버스 백버퍼 픽셀 — 채우기 비용의 직접 지표 |

보고서에는 UA·DPR·zoom·백버퍼·코어 수·메모리와 **초당 기록(스테이지별)** 이 함께 담긴다.
어느 구간에서 무너지는지 그대로 드러난다.

파라미터가 없으면 아무것도 로드되지 않으므로 일반 사용자에게는 영향이 없다.

---

## 4. 확인된 것 — 8px 텍스트의 정체

```css
/* blocks/0018_anon.css */
.tc-btn-label { font-size: 8px; font-weight: 800; opacity: 0.85; }
.tc-btn-key   { font-size: 8px; }
```

터치 컨트롤 버튼의 **라벨(«대화» 등)** 과 **키 힌트 배지**다.

여기에 `body { zoom: 0.65 }` 가 곱해지므로 **화면에는 5.2px 로 렌더된다.**
실측 라벨 박스가 10x8px 였다. 사실상 판독 불가다.

`opacity: 0.85` 까지 겹쳐 대비도 낮다. 최소 11~12px + `opacity: 1` 을 권한다.
다만 버튼(51x51) 안에서 넘칠 수 있어 아이콘 크기 조정이 함께 필요하다 — **미적용, 논의 필요.**

---

## 5. 제안 — 세로 방향 안내 (미적용)

현재는 세로로 잡으면 «기기를 가로로 돌려주세요» 오버레이만 뜬다. 개선안:

**Android** — 전체화면 진입 «직후에만» 방향 잠금이 가능하다. 넣으면 안내가 아예 안 뜬다.
```js
await screen.orientation.lock('landscape');   // requestFullscreen() 성공 뒤에 호출
```

**iPhone** — 방향 잠금도 Fullscreen API 도 지원하지 않는다. 설치 유도도 안 하기로 했으므로
**안내 오버레이가 유일한 수단**이다. 그렇다면 오버레이 자체를 개선하는 게 맞다:

1. 정적인 문구 대신 **회전하는 폰 아이콘 애니메이션** — 무엇을 하라는지 즉시 전달된다
2. **세로에서도 게임이 계속 돌게** 두고 오버레이를 반투명으로 — 지금은 완전히 가려 «멈춘 것»처럼 보인다
3. iOS 에서 «화면 회전 잠금»이 켜져 있으면 돌려도 안 바뀐다. 이 경우를 감지해
   **«제어센터에서 회전 잠금을 꺼주세요»** 로 문구를 바꾼다 — 실제로 가장 흔한 막힘 지점이다

3번이 특히 중요하다. 회전 잠금을 켜 둔 사용자는 아무리 돌려도 진행이 안 되는데,
현재 문구로는 원인을 알 수 없다.

---

## 6. 남은 것

| 항목 | 상태 |
|---|---|
| 캔버스 백버퍼 상한 (1번) | 논리 해상도 결합도 확인 후 |
| `visualViewport` 연동 | 미착수 — 주소창 대응의 핵심 |
| 전역 UI 스케일 (`--ui` 변수) | 미착수 — px 735 / % 126, 브레이크포인트 10개+ |
| 캐릭터 생성 모달 버튼 잘림 | **미수정** — 뷰포트 높이 360 미만에서 `모험 시작` 이 접힘 |
| 8px 라벨 (4번) | 논의 필요 |
| 세로 안내 개선 (5번) | 논의 필요 |
| 런타임 FPS 원인 규명 | **실기기 `?perf=1` 결과 대기** |

### 테스트 기기에 대한 주의

갤럭시 탭 S10 과 iPhone 17 Pro 는 **최상급 기기**다. 목표가 «적당한 폰»이라면
이 둘에서는 버벅임이 재현되지 않을 가능성이 높다. 두 기기는 **화질·레이아웃 검증용**으로 쓰고,
성능은 중급기(예: 갤럭시 A 시리즈)에서 `?perf=1` 로 한 번 재는 것이 정확하다.

다만 두 기기로만 확인할 수 있는 것도 있다:
- **iPhone 17 Pro** — Safari 가 Fullscreen API 를 지원하는지 직접 확인 가능
  (콘솔에서 `!!document.documentElement.requestFullscreen`)
- **갤럭시 탭 S10** — 1번의 «태블릿에서 흐림» 가설 검증
