# 웹판 로딩 최적화 (v398)

모바일에서 초기 로딩이 20초를 넘고 버벅이던 문제를 잡은 작업 기록.

## 1. 무엇이 문제였나

배포본(https://bongdam-safety.netlify.app) 실측 — 2026-09-05:

| 항목 | 데스크탑 | 모바일 |
|---|---|---|
| load 이벤트 완료 | 37.2 초 | 24.2 초 |
| 요청 수 | 174 | 174 |
| 전송량 | 7.39 MB | 7.39 MB |

요청 하나를 타이밍 분해하면 원인이 드러난다:

```
dbd03fea_asset.png (13KB)
  stalled  (큐 대기) : 14,611 ms   ← 여기서 다 잡아먹힘
  waiting  (서버응답):    596 ms
  download (다운로드):      1 ms   ← 네트워크는 멀쩡
```

13KB 파일이 1ms 만에 받아지는데 15초를 대기했다. 프로토콜은 HTTP/2 라
연결 수 제한도 아니다. **대역폭이 아니라 요청 큐·메인 스레드 포화**가 원인이었다.

### 근본 원인

`webbuild.js` 의 externalize 는 `data:image/png;base64,@@B64:NAME@@` 를
`assets/NAME` 로 바꾼다. 그런데 소스 곳곳에 이런 코드가 있다:

```js
// src/blocks/0017_anon.js 등 — 최상위 스코프
const _npcSpriteImg = new Image();
_npcSpriteImg.src = _NPC_SPRITE_PNG;          // 개발 빌드: data URI (요청 없음)
                                              // 웹 빌드:  assets/... (즉시 요청!)
const _maleIdleImgs = MALE_IDLE_FRAMES.map(function(src){
  var im = new Image(); im.src = src; return im;   // 프레임 수만큼 즉시 요청
});
```

개발 빌드는 data URI 라 아무 문제가 없다. 웹 빌드에서만, 스크립트가 파싱되는
순간 **174개 요청이 한꺼번에 발사**되어 서로를 막았다.

## 2. 무엇을 했나

### (1) 이미지 지연 로딩 — `검수도구/tools/web_lazyimg.js`

`assets/` 로 시작하는 `img.src` 대입을 보류했다가 **실제로 그리려는 순간**
(`drawImage`) 발사한다. 부팅이 끝나면 남은 것을 유휴 시간에 조금씩 채운다.

소스 블록은 **한 줄도 고치지 않았다.** `webbuild.js` 가 웹 빌드에만 주입하므로
개발 빌드와 인게임 에디터는 그대로다. 블록 번호 재정렬도 없다.

보류하면 안 되는 경우를 지킨다:

| 조건 | 이유 |
|---|---|
| `onload`/`onerror` 가 걸린 이미지 | `loadAllImages` 는 remaining 카운터가 0이 될 때 콜백을 부른다. 하나라도 보류하면 **게임이 영영 시작되지 않는다.** |
| DOM 에 붙어 있는 `<img>` | 캔버스로 그려지지 않아 `drawImage` 가 발사할 일이 없다. `#dialogue-portrait` 가 여기 해당 — 보류하면 대사 초상화가 안 뜬다. |
| `decode()` 호출 / `addEventListener('load')` | 누군가 완료를 기다리기 시작했다는 신호 |

`complete` 게터도 함께 덮어 보류 중에는 `false` 를 반환한다.
`if (img.complete && img.naturalWidth)` 형태의 가드가 오작동하지 않게 하기 위함이다.

### (2) 폰트 서브셋 — `검수도구/tools/font_subset.js`

임베드된 Noto Serif KR 3종(400/700/900)은 KS X 1001 2350자 기준 **903KB** 였다.
게임이 실제로 쓰는 한글은 **1,034자**뿐이라 서브셋으로 **505KB (-44%)** 가 됐다.

`#hero-name` 으로 플레이어가 임의의 한글 이름을 넣을 수 있으므로,
서브셋 `@font-face` 에 `unicode-range` 로 담당 글자를 명시했다. 범위 밖 글자는
이 폰트가 관여하지 않고 스택의 다음 항목으로 넘어간다 —
게임 스택이 `"Noto Serif KR", serif` 라 **시스템 한글 명조로 정상 렌더된다(두부 아님).**

> ⚠️ 전체 폰트를 `unicode-range` 없이 함께 선언하면 안 된다. 두 face 가 같은 글자를
> 중복 담당해 브라우저가 6종을 전부 내려받는다. 실측 1,407KB 로 **개선이 아니라 악화**였다.

원본 폰트 3개는 `src/assets/` 에 그대로 둔다 — 서브셋 재생성의 입력이다.
웹 빌드는 참조되지 않는 에셋을 복사하지 않으므로 배포본에는 포함되지 않는다.

## 3. 결과

로컬 동일 조건(모바일 에뮬레이션, 서비스워커 없음, 같은 머신·같은 경로) 비교:

| 지표 | 원본 | 최적화 | 개선 |
|---|---|---|---|
| **초기 요청 수** | **128** | **5** | **-96%** |
| **초기 전송량** | **4,477 KB** | **606 KB** | **-86%** |
| 3초까지 누적 요청 | 136 | 10 | -93% |
| load 이벤트 | 379 ms | 238 ms | -37% |
| 폰트 | 903 KB | 505 KB | -44% |
| 전체 전송량 | 5.58 MB | 5.19 MB | -7% |

전체 전송량이 크게 안 준 것은 의도한 결과다. 같은 에셋을 **부팅 경로에서 치워
배경으로 옮긴 것**이지 지우지 않았다. localhost 는 지연이 0이라 벽시계 차이가
작게 보이지만, 실제 모바일에서는 요청 하나당 15초씩 밀리던 구간이 사라진다.

부팅 후 실제로 필요하지 않았던 에셋이 **23개** 남았다(`stillPending`) —
원본은 이것들도 전부 받고 있었다.

### 검증한 것

- 타이틀 → 캐릭터 선택 → 프롤로그 필드(스테이지 101) 진입까지 정상
- 실패 요청 0건, DOM 보류 이미지 0개
- 서브셋 밖 글자 렌더 폭 > 0 (폴백 정상, 두부 아님)
- 개발 빌드(`bundle.js`) 정상 — 미치환 토큰 0

### 아직 안 한 것

- **런타임 FPS / 프레임 드랍은 측정하지 못했다.** 측정 환경에서 창이 비활성이라
  `requestAnimationFrame` 이 돌지 않았다. 이 문서의 수치는 전부 로딩 구간이다.
- 스프라이트 아틀라스(PNG 119개 → 2~3장) 미적용. 지연 로딩으로 부팅 경로에서
  빠졌기 때문에 우선순위가 내려갔다.
- 지도 WebP 재압축(2.2MB) 미적용. 마찬가지로 이제 해당 지역 진입 시에만 받는다.

## 4. 유지보수

### 텍스트를 많이 추가·수정했을 때

새로 쓴 글자가 서브셋에 없으면 그 글자만 시스템 폰트로 보인다. 재생성:

```bash
node 검수도구/tools/font_subset.js src
# 출력된 _font_face.generated.css 를 src/blocks/0005_bd-embedded-font.css 에 반영
```

필요 환경은 `py -m pip install --user fonttools brotli` 다.
**빌드 자체에는 Python 이 필요 없다** — 서브셋 파일이 이미 저장소에 있다.

### release.js 를 돌리기 전에 (중요)

`release.js` 1단계는 `src ↔ HTML 일치 검사`다. src 를 고쳤으므로 손에 있는
작업용 HTML 과 어긋난 상태다. 여기서 **`html` 기준을 고르면 src 가 옛 내용으로
덮여 이 최적화가 통째로 사라진다.** 먼저 src 기준으로 HTML 을 다시 만들어라:

```bash
node 검수도구/tools/bundle.js src 봉담지킴이_게시용_v338_final.html
```

### src/blocks/ 에 임시 파일을 두지 말 것

빌더는 `^(\d{4})_` 로 블록을 잡는다. `0005_....css.bak` 같은 파일을 같은 폴더에
두면 그게 0005 로 잡혀 **변경이 조용히 무시된다.** 실제로 이 작업 중에 겪었다.

### 디버그

```js
__BD_LAZY.stats      // { deferred, armed, eager }
__BD_LAZY.pending()  // 아직 안 받은 개수
__BD_LAZY.flush()    // 전부 즉시 받기 (QA 용)
```
