# 성능 2차 (v399, 2026-09-12) — 에셋 다이어트 · JS 외부 분리

`성능최적화_v398.md` 의 «아직 안 한 것» 세 가지를 실측으로 결정한 기록. 측정 도구는 전부 검수도구에 있다.

| 항목 | 결정 | 근거 |
|---|---|---|
| 지도 WebP 재압축 | **기각** | 이미 q85 손실이라 q0.8 재인코딩 이득 11~21%, 세대 손실만 남는다 |
| 건물 PNG → WebP | **채택** (45장, -5.5MB) | 547×547 에 492KB 같은 PNG 가 41장. q0.9 손실 WebP 로 76~95% 감소, 육안 구분 불가 |
| JS 외부 분리(splitjs) | **기본 켬** | v398 회귀 원인(마크업 핸들러가 번들 전 호출)을 빌드 시 스텁+재생으로 닫음 |
| 스프라이트 아틀라스 | **보류** | 아래 «하지 않은 것» |
| 오디오 재인코딩 | **보류** | 인코더 없음(Playwright ffmpeg 는 데모용 최소 빌드) + 사용자 제공 음원이라 별도 결정 |

## 1. 건물 PNG → WebP (`검수도구/tools/recompress_webp.cjs`)

이 PC 에 cwebp·sharp·ImageMagick 이 없어 **Chromium(Playwright) 의 canvas.toBlob('image/webp', q)** 로 인코딩했다.
libwebp 라 결과는 cwebp 와 같다. 새 의존성 없음.

```bash
node 검수도구/tools/recompress_webp.cjs src png --dry --q=0.9      # 대상·이득·PSNR 만 출력
node 검수도구/tools/recompress_webp.cjs src png --q=0.9 --min-psnr=30   # 적용
```

- 대상: 40KB 이상 PNG 45장 (0097 건물 41 · 0002/0003 건물 2 · 0104 NPC 초상 2)
- 화질: 알파 가중 PSNR 30~38dB, 알파 채널은 무손실(99dB). ×1·×0.4 배율 나란히 그려 비교 — 구분 불가
  (`검수도구/shots_polish399/webp_ab.png`). 게임에서 건물은 원본의 30~60% 크기로 그려진다.
- 이름 규칙: 에셋 이름은 내용 해시(`unbundle.js` 규칙)라 **새 해시로 재발급**하고 블록의 토큰·mime 접두어
  (`data:image/png;base64,` → `webp`)를 함께 바꿨다. 이름을 유지하면 immutable 캐시가 낡은 파일을 낸다.
- 베이크(0002) 영역이 바뀌므로 **restamp** — `bd-bake-60637abc-3145728`.
- 결과: 작업용 번들 44.4 → 35.6MB, 웹 `assets/` 28.4 → 24MB (부팅 경로는 무변화 — 건물은 지역 진입 시 받는다).

무손실 WebP(q=1)도 재 봤다: 15~20% 밖에 안 줄어 의미 없음. 지도 4장(2.2MB)은 위 표대로 기각.

## 2. 죽은 배경 CSS (`#game-bg`, 487KB)

`0006_anon.css` 의 `#game-bg { background:url(...) }` — **이 id 의 요소는 어디에도 만들어지지 않는다.**
브라우저는 요소 없는 배경을 요청하지 않아 실사용엔 영향이 없었지만, 웹 `assets/` 와 작업용 번들(base64 650KB)에는
실려 나갔다. 규칙을 지우니 에셋도 빠졌다. 같은 이유로 미참조 `sub900.woff2`(136KB)도 제거. `build.js` 는 이제
`dist/assets/` 를 먼저 비운다 — webbuild 는 «쓰는 것만 복사»하고 지우지는 않아 옛 파일이 main 까지 올라갔었다.

## 3. JS 외부 분리 (splitjs) — 기본 켬

### 무엇이 달라지나

| | 분리 전 | 분리 후 |
|---|---|---|
| index.html | 2,693KB (brotli 429KB) | **276KB (brotli 52KB)** |
| game.js | — | 2,407KB (brotli 389KB), 해시 이름 → 장기 캐시 |
| 첫 방문 전송량 | 429KB | 441KB (±) |
| 재방문·«시작하기» 리로드 | 429KB 다시 | **52KB 만** |

첫 방문은 같다. 이득은 **문서(no-cache)가 가벼워지는 것**이다. 특히 «시작하기»는 0154 가 purge→reload 를 하므로
새 게임마다 문서를 한 번 더 받는다 — 실측(s_bootbytes)에서 타이틀→필드 사이에 index.html 이 두 번 잡혔다.

### v398 회귀의 진짜 원인과 닫은 방법

v398 은 두 번 회귀를 겪고 분리를 껐다. 기록된 원인은 «마크업은 파싱됐는데 핸들러 함수는 아직» 창이었다.
BOOT_GUARD(pointer-events:none)로 실사용자는 막았지만, **하네스는 element.click() 으로 누르므로 잠금을 우회**해
`ReferenceError: selectCharacter is not defined` 가 났다.

해법 — `splitjs.js` 가 빌드 때:
1. 마크업(스크립트 본문 제외)의 `on*="…"` 에서 호출되는 함수 이름을 뽑는다 (19개: closeModal·selectCharacter·advanceDialogue …).
   `window.X&&window.X()` 로 이미 보호된 호출은 제외 — 준비 전엔 조용히 무시되는 것이 의도다.
2. `<head>` 에 **스텁**을 깐다: 그 이름들이 아직 없으면 «호출을 큐에 넣는 함수»로 정의.
3. 번들 끝에서 **재생**한다: 큐를 진짜 함수로 다시 부른다. 번들의 최상위 `function` 선언이 같은 이름의
   window 속성을 덮으므로 스텁은 자연히 사라진다(let/const 정의는 전역 스코프에서 이름을 직접 평가).

검증 `검수도구/s_split_v399.cjs`: game.js 응답을 4초 지연시키고 그 사이에 `closeModal('continue'); selectCharacter(2)`
를 호출 → 예외 없이 큐 2건 → 번들 뒤 재생 완료·스텁 교체·타이틀 표시·리로드 경로·콘솔 0. 14/14.

끄기: `BD_SPLIT=0 node build.js`.

## 4. 하지 않은 것 — 스프라이트 아틀라스

- 작은 PNG 134장(1.35MB)은 지연 로딩(v398)으로 **부팅 경로에서 이미 빠져** 유휴 시간에 받는다. 남는 이득은
  «배경 요청 수»뿐이고 GitHub Pages 는 HTTP/2 라 요청 수 자체의 비용이 작다.
- 반면 주입 난이도는 높다: `drawImage` 프로토타입을 이미 둘(web_lazyimg·0098 픽셀 정합)이 감싸고 있어 래핑 순서를
  고정해야 하고, `0017:3073` 은 `getImageData` 로 알파를 읽어 좌표계가 달라지며, 0017:1353 은 1.5초 뒤 src 를
  런타임 교체한다. 인게임 에디터의 사용자 업로드 이미지는 아틀라스에 없어 폴백이 필요하다.
- «위험 없는 이득만 취한다»(v398 원칙)에 따라 보류. 실기기 `?perf=1` 결과에서 지역 진입 팝인이 문제로 나오면 그때 재검토.

## 5. 측정 도구

| 도구 | 용도 |
|---|---|
| `검수도구/s_bootbytes_v399.cjs` | 타이틀·필드 진입까지 요청 수/바이트, 타이틀 이후 큰 응답 |
| `검수도구/s_split_v399.cjs` | 분리 빌드 스텁·재생·리로드 경로 |
| `검수도구/tools/recompress_webp.cjs` | 이미지 재인코딩 + 참조 갱신 (`--dry` 로 먼저) |
| `tools/serve.cjs` (저장소 밖 `bongdam/tools`) | `/new/`=웹게시, `/base/`=`_base`(이전 배포본 `git archive origin/main`) 비교 서빙 |
