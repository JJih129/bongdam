# 봉담지킴이 — 최종 개선 구현 계획 (v280 → v290 목표)

> 전제 문서: [스토리_대사_전수분석_v280.md](스토리_대사_전수분석_v280.md)
> 원칙: 단일 HTML + 레이어 추가 방식 유지("뒤에 오는 것이 이긴다"), 매 단계마다 검수도구 무인 완주 회귀.
> 순서가 곧 의존 관계다 — 0단계를 건너뛰면 이후 단계의 분모·대사가 전부 틀어진다.

---

## 단계 0 — 배치 데이터 세탁 (선행 필수, 반나절)

**목표**: 신규 플레이가 "깨끗한 봉담"에서 시작하게.

| # | 작업 | 대상 |
|---|---|---|
| 0-1 | 런타임 오염 필드 일괄 제거: `__bdGone`, `__bdHzQuestLines`, `__bdOrigLines`, `__bdColCleared`, `_talkIdx`, `solid:false`(위험요소), `__bdInfoV69` 등 `__bd*` 접두 전부 | `bongdam_rpg_editor_data_v5_2_quest.json` → 재베이크(`__BD_BAKED_STAGE_RAW`) |
| 0-2 | "복사" 라벨 정리: `재이 복사`→`재이`, `재현 복사`→`재현`, 시설 6곳(`어울림공원 복사` 등) | 같은 JSON |
| 0-3 | 수영리(210)의 「와우리문화공원 복사」 오배치 제거 | 같은 JSON |
| 0-4 | `_editorId` 중복 재발급 (213 두 쌍, 101 소파 7개·테라스 9개 등) | 같은 JSON |
| 0-5 | `hazardFamily` 를 변형 정의(fam)와 일치시키기 (kickboard/glass/bottle) | 같은 JSON |

**검증**: 새 게임 → 상리 진입 → 「버려진 술병」 존재 확인 / 주민 6명 첫 대화가 부탁·기본 대사인지 확인 / 무인 완주 1회.

---

## 단계 1 — 진행 축 단일화 (구조 수술, 1일)

**목표**: "지도 %"를 유일한 진실로. 엔딩·보스 관련 A급 버그 4건이 여기서 소멸.

| # | 작업 | 내용 |
|---|---|---|
| 1-1 | `BD_MapProgress` 신설 (새 레이어 `bd-safety-map-core-v282`) | 지역별 % 재계산기. 세이브 필드 추가 없음(파생값). 배점: 부탁 완결 40 / 정화 25 / 시설 스탬프 20 / 시설 방문·안내 10 / 주민 인사 5, 항목별 실개수로 정규화 |
| 1-2 | `BD_finaleOpen()` 단일 함수 | `4개 지역 % == 100` 하나로. `BD_hazardLocked`·`BD_hazardAllowed`·`hazardInteract`·`104` 보스 조우가 전부 이것만 참조. 개방 시 `BD.questIdx = 5` 승급(챕터 카운트 오염 방지) |
| 1-3 | 엔딩 단일화 | `onHazardBattleEnd`의 엔딩 대사(선생님·담이 6줄)를 `final_done` 컷신 앞부분에 병합 → 보스 승리 = `final_done`(통합본) → `showEnding()` 한 경로. "활동지도" 표기 → "안전 지도" 통일 |
| 1-4 | 보스 미처치 가드 | `grantReward('final')`에서 `_bossDone` 아니면 컷신·모달·보상 전부 중단 (현재는 목표 수치만 리셋) |
| 1-5 | 이중 토스트 dead branch 제거 | `030:1046-1054` else 가지 삭제 |
| 1-6 | 챕터 `need` 로드 레이스 제거 | 로드 직후 `syncNeeds()` 즉시 1회 호출(1.5초 폴링 대기 전) |

**검증**: ① 정상 루트 완주 ② 스탬프 루트 조기 보스(스탬프6+조각4, 부탁 미완) → 챕터 오염 없이 최종장 진입 ③ final 챕터에서 일반 정화만으로 엔딩 미발생 ④ 엔딩 대사 1벌만 출력.

---

## 단계 2 — 봉담 안전지도 UI (신규 핵심 기능, 1~2일)

**목표**: "돌아다니며 지도를 활성화하고, 지역 100%마다 스킬, 보스로 지도 완성" — 게임 정체성 확립.

| # | 작업 | 내용 |
|---|---|---|
| 2-1 | 약도 SVG 레이어 `bd-safety-map-ui-v282` | 인라인 SVG 약도(외부 에셋 0): 중앙 문화의집 + 4개 리 블록 + 도로 연결선. 지역 블록에 위험요소 점(붉음→✨)·시설 아이콘(흐림→컬러) |
| 2-2 | 비활성 연출 | 지역 `filter: grayscale(1-p) brightness(.6+.4p)` — %에 비례해 색이 살아남. 잠긴 지역 자물쇠 |
| 2-3 | % 표기 | 지역별 진행 링 + 0~100%, 상단 전체 진행률, 중앙 보스 칸 "???" → 4지역 100% 시 활성 |
| 2-4 | 담이 = 지도의 화자 | 배지 클릭·M키로 열기. 헤더에 담이 얼굴 + 추천 목표 1줄(`BD_askDami` 로직 흡수: "상리 지도가 60%예요. 봉담도서관을 아직 안 가봤어요!") |
| 2-5 | 스킬 획득 재배선 | fan/wash/cheer/light 지급을 questIdx 챕터 보상 → **지역 100% 도달**로 이동. 지도에서 지역이 완성되는 연출과 함께 지급 |
| 2-6 | 지도 알림의 담이 이관 | 🏅 스탬프·🧩 조각·🗂 카드 획득을 시스템 토스트 → 담이 말풍선("제 지도에 「○○」이 새겨졌어요!")으로. 토스트는 골드 등 수치만 |
| 2-7 | 엔딩 연결 | `showEnding()`의 카드 나열부를 완성된 지도(전 지역 컬러 100%)로 교체 — "지도 완성"이 시각적으로 닫힘 |

**검증**: 신규 s_map.js(각 이벤트 후 % 스냅샷 비교) + 지역 100% 시 스킬 토스트·지도 연출 확인 + 구세이브 로드 시 % 정상 재계산.

---

## 단계 3 — 컷신·담이 연출 통합 (1일)

**목표**: "사람 이야기는 VN, 담이는 말풍선 하나, 숫자는 토스트."

| # | 작업 | 내용 |
|---|---|---|
| 3-1 | `playScene` 인터리브화 | 담이 줄을 "끝에 한 덩어리" → 컷신 진행 순서대로 말풍선 줄별 출력(기존 `playDamiLines` 재사용). `SCENARIO`에 `face` 필드 지원 |
| 3-2 | 도입 컷신 부활 | `ch2~ch4_intro` + `final_intro`를 해당 지역/최종장 **첫 진입 시 1회** 재생(`playSceneOnce`). `final_intro` 마지막 줄 "페이즈마다 약점 속성이 바뀐다" → "물러날 수 없다. 조심해!" 수정. `prologue_intro`는 현행 프롤로그와 설정 충돌 → 삭제 |
| 3-3 | 담이 지역 첫인상 추가 | ch2: "공원이 많은 동네네요! 그런데… 벤치 쪽에서 차가운 기운이 느껴져요."(💦) / ch3: "아이들 웃음소리가 들려요. 이 거리는 꼭 지켜주고 싶어요."(🔰) / ch4: "해가 지고 있어요. 어두운 곳일수록 그림자가 힘을 내요 — 서둘러요!"(💦) / final: "…이 기운, 지금까지 중 제일 커요. 그래도 우리라면 할 수 있어요."(❗→✨) |
| 3-4 | 담이 발화 큐 | 우선순위: 튜토 > 스토리 > 상황 반응 > 한담. 같은/낮은 순위는 타이핑 완료 후 순차, 높은 순위만 즉시 교체. `BD_DAMI.show` 래핑 레이어 |
| 3-5 | 말풍선 단일화 | 097 크림색 말풍선 제거 → `BD_DAMI.show` 통일. 회복 안내 3중 출력 → 1회. autoFold를 `max(5200, 글자수×85+1500)ms` |
| 3-6 | grantReward 타임라인 재배치 | 대사(독백·전화·컷신) 먼저 → 보상 토스트 뒤로. 4장의 "👑 보스 등장" 토스트를 "(광장 쪽 기운이…)" 독백 뒤로 지연 |
| 3-7 | 죽은 스킵 버튼 3개 레이어 잔재 제거 | 059 CSS·바인딩·표시 제어 + 114 은닉 레이어 |

**검증**: 챕터 전환 4회를 눈으로 재생 확인(스크린샷) + 무인 완주 회귀.

---

## 단계 4 — 전 표면 텍스트 첨삭 (반나절~1일)

**목표**: 용어·톤 통일 + 개별 오류 수정. (세부 «원문→수정안» 표는 텍스트 감사 완료 후 본 문서 부록으로 추가)

### 4-A. 용어 통일 규칙 (전 표면 공통)
| 항목 | 통일안 | 현재 혼용 |
|---|---|---|
| 배지 이름 | **봉담 활동 배지** (아이템·연출·토스트 포함) | 지킴이 배지 |
| 적 호칭 | **불안의 그림자 / 위험 요소** | 몬스터(041 토스트) |
| E키 스킬 | **배지 스킬** | 안전 스킬(034) |
| 최종 산출물 | **봉담 안전 지도** | 봉담 활동지도(엔딩 A경로) |
| 사서 | **사서 도현** (화자명 정본 수정, 정규식 치환 폐기) | 사서 정도현 |
| 화폐 | **G** 표기 + "소지금" | 골드/소지금/G 혼용 |
| 도서관 | 와우리=**봉담와우도서관**(붙여쓰기), 상리=**봉담도서관** | 봉담 와우 도서관 등 변형 |

### 4-B. 확정된 개별 수정 (감사 완료분)
| 위치 | 원문 → 수정안 |
|---|---|
| 043 곤란 말풍선 | 키 `crack`→`road_crack`, `sign`→`sign_ghost` (전용 대사 2종 복구) |
| 041 첫 전투 토스트 | "몬스터마다 약한 속성이…" → "그림자마다 약한 속성이 있어요 — 약점을 치면 피해 1.5배!" (담이 튜토와 중복이므로 튜토 미시청자에게만) |
| 030 정화 후 재조사 | 키를 `ch1_*` → `hazardVariant` 기준으로 교체(9종 대사 부활 + bicycle 추가) |
| 083 사서 도현 | "도서관은 1·2층이고, 3층은 청소년문화의집이에요" → "옆 희망카페도 같은 건물이에요" (상리 봉담도서관 사실관계) |
| 059/135 간식 | 토스트 "담이가 간식을 챙겨왔어요" → "선생님이 챙겨 주신 간식이에요!" |
| 075 상점 튜토 | 게이트 `bd_dami_tutorial_done` → `BD.purified['ow212_trash_1']` ("첫 정화 완료!" 거짓 방지) + 용돈 50G → 지역 상점 최저가에 맞춰 동적 지급 |
| 059 wash 중복 | `SKILL_INTRO.wash[2]` → "이번에도 물줄기를 얼룩에 계속! 감 잡았죠?" |
| 030 임무 desc | 인물명 하드코딩 제거 → `BD_hzQuestMap()`에서 동적 생성 |
| 095 전화 연출 | 2장: 재현 소개 문구를 "재현이 알지? 걔가 동화리에서…"로 / 3장: "내 친구 은지 어머니가" → "은지네 어머니가" |
| 030 타이틀 | `bd-title-reset` id·모바일 폴백 "🔄 초기화" → "종료하기"로 일치 |
| 101/135 터치 치환 | 정규식 단어 치환 폐기 → 문장 단위 통치환 확대("조이스틱나" 파괴 제거, "(ESC)"→"(물러나기 버튼)") |

### 4-C. 텍스트 감사 결과 반영 (부록 예정)
전투/미니게임·메뉴/HUD/업적·상점/아이템·시설 모달 4개 표면의 전수 감사가 완료되는 대로 «원문→수정안» 표를 본 문서 부록으로 추가하고 일괄 적용.

---

## 단계 5 — 죽은 코드 청소 (반나절, 마지막)

수정이 아니라 **삭제**. 파일 크기·폴링 부담 감소 + 유지보수성.

- 현지 서사(044) + 차단 레이어(085)의 차단 대상 데이터 / 임현지·정도현 데모 퀘스트 분기 / 구 튜토리얼 `TUT_STEPS` + 설정의 무효 "튜토리얼 다시 보기" 버튼 / `NPC_LINES`·`npcTalk`(따옴표 버그 포함) / [배지 통신] 퀘스트 6종(은폐 레이어 084 포함) / 지킴이 유형 3종(살릴 계획 없으면) / 증강 「균열 확대」·「넉넉한 준비」(효과 구현 또는 풀에서 제거) / `prologue_intro`·구 UI 캐릭터 선택(007) / 스토리 레이어 상시 폴링 타이머 정리(`BD_addTick` 통합)

---

## 작업 순서와 리스크 요약

| 단계 | 규모 | 리스크 | 산출 버전 |
|---|---|---|---|
| 0 데이터 세탁 | 반나절 | 낮음(스크립트 검증 가능) | v281-clean |
| 1 진행 축 단일화 | 1일 | **중간** (게이트 로직 — 회귀 필수) | v283 |
| 2 안전지도 UI | 1~2일 | 낮음(표시 위주) + 2-5 재배선만 중간 | v285 |
| 3 컷신·담이 통합 | 1일 | 중간(연출 타이밍) | v287 |
| 4 텍스트 첨삭 | 반나절~1일 | 낮음 | v288 |
| 5 죽은 코드 청소 | 반나절 | 낮음(삭제 후 회귀 1회) | v290 |

**매 단계 공통 검증**: `검수도구/drive.js s_auto.js` 무인 완주(막힘 0·콘솔 0) + 해당 단계 전용 시나리오. 터치 모드(`--touch`)는 단계 4 후 필수 1회.

**권장 커트라인**: 시간이 없으면 **0 → 1 → 4-B**까지만 해도 "게임이 올바르게 끝나는" 상태가 된다. 2·3은 게임을 "봉담 안전지도 대작전"답게 만드는 단계다.
